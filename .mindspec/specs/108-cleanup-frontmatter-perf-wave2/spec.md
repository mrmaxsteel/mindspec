---
status: Draft
approved_at: ""
approved_by: ""
---
# Spec 108-cleanup-frontmatter-perf-wave2: Cleanup wave 2: frontmatter unification, validation perf caching, deferred trace/golangci ownership

## Goal

Land the second bounded cleanup wave from the 2026-07-02 repo review (`review-source-report.md`, §4 order 4–5 plus the two ownership-gated items wave 1 explicitly deferred), scoped to exactly four work areas: (1) claim the two unowned paths wave 1 could not touch — `internal/trace/**` and `.golangci.yml` — in the workflow domain's `OWNERSHIP.yaml`, then delete the dead `trace.Event.MarshalJSON` and drop the two now-inert `unparam` carve-outs those claims unblock (a third, `findRoot`, waits on wave 1's deletion of it — see Out of Scope); (2) collapse the re-implemented YAML-frontmatter fence scanners onto the canonical `internal/frontmatter` package (report §2 DRY #3), including replacing the `## Approval` prose-scan with the frontmatter-based `validate.SpecStatus` so a spec has one source of approval truth; (3) hoist per-`(file × domain)` `OWNERSHIP.yaml` loading and memoize per-run ADR parsing in `internal/validate` (report §3 Perf #1 + #4); (4) make `doctor`'s dead-manifest check walk the repo tree once instead of once per domain (report §3 Perf #5). Target outcome: one source of truth for frontmatter and approval status (a documented ZFC correctness fix), materially fewer file reads and git subprocesses on the validation and doctor hot paths, and two previously-unowned paths brought under a domain — with byte-identical externally observable behavior everywhere except the deliberately-tightened frontmatter fence strictness (resolved to the canonical package's semantics) and the corrected approval-status source of truth.

## Background

The 2026-07-02 review swept 341 tracked Go files with `deadcode` (with and without `-test`), `golangci-lint`, and four subsystem review agents, verifying every finding against source (the panel-local tracked copy is `review-source-report.md`). It ranked a cleanup order (§4). Wave 1 (spec 107, `review/spec-107-approve/source-report.md` §4 order 1–3) landed the dead-code sweep, setup managed-block unification, and the `complete` perf pair, and **explicitly deferred to wave 2** the frontmatter consolidation, the ownership/ADR validation caching, the doctor per-domain walk, the `internal/trace` dead-symbol deletion, and the stale `.golangci.yml` `unparam` carve-out removals. It deferred the trace and `.golangci.yml` edits because `internal/trace` is claimed by zero manifests and `.golangci.yml` is a non-doc file matching no `OWNERSHIP.yaml` glob (and, unlike `AGENTS.md`, it is not in `isDocFile`'s `rootOperatorDocs` allowlist), so editing either path while unowned would trip `adr-divergence-unowned` at `mindspec complete` (wave-1 spec 107 § Out of Scope). This spec is **wave 2**: it lands those deferrals by claiming the paths *first*, then makes the deferred frontmatter and perf changes.

- **Frontmatter re-implementation (§2 DRY #3)** is the highest-correctness item: `internal/frontmatter.Parse` is documented as the single implementation ("callers must NOT re-implement… they will silently drift"), yet `internal/approve/plan.go` carries two ~40-line read-mutate-rewrite scanners (`updatePlanApproval`, `writeBeadIDsToFrontmatter`) and four more sites hand-roll the `---` fence scan (`approve/spec.go` `splitFrontmatter`, `approve/impl.go` `readPlanBeadIDs`, `contextpack/budgeter.go` `parseCitedADRs` + the `extractPlanBeadSection` strip, `validate/plan.go` `parsePlanFrontmatter`). The drift is real and observable: the canonical `Parse` requires `strings.TrimRight(line, "\r\n") == "---"` while the hand-rolled copies accept a `strings.TrimSpace`d fence, so a space-padded fence line parses differently depending on which copy runs. Separately, `validate/state.go` `readSpecApprovalStatus` substring-scans the `## Approval` prose that the YAML-frontmatter `validate.SpecStatus` was created to replace — two sources of truth that can disagree on the same `spec.md` (the ZFC violation the `frontmatter` package documents against).
- **Validation perf caching (§3 Perf #1 + #4)** targets the divergence hot path: `attributeDomain` calls `loadOwnershipForRef` once per domain, invoked once per changed file, so one gate run does `N × D` manifest parses — each a `ReadFile` + `yaml.Unmarshal`, or up to three `git show` subprocesses per domain when reading at a ref (`LoadOwnershipAtRef`). `checkUnclaimedSource` (`docsync.go:220-230`) already shows the fix: load every manifest once, attribute in memory. In the same file family, plan validation re-parses cited ADRs `O(domains × citations)` because `coverageOf`/`hasAcceptedCitation`/`checkADRCitations` each call `store.Get` per citation per domain and every `Get` re-reads and re-parses the ADR file.
- **Doctor per-domain walk (§3 Perf #5)** is a self-contained fix: `manifestResolvesAny` (`doctor/ownership.go:121-153`) does a full `filepath.WalkDir(root)` per domain, and the dead-manifest case it exists to catch always walks to completion → `O(domains × repo files)`.

Everything else the review surfaced (the remaining DRY items #2/#4/#6–#18 and Perf #2/#3/#6/#7) is deliberately **out of scope** (see Out of Scope) so this wave stays bounded and independently reviewable against the report.

## Impacted Domains

- **workflow** — owns all four work areas' edits: `internal/approve/**` (frontmatter mutate/read consolidation), `internal/validate/**` (frontmatter reader migration, `readSpecApprovalStatus` replacement, `OWNERSHIP.yaml` load hoisting, ADR-parse memoization), `internal/doctor/**` (tree-walk-once), `internal/adr/**` (the memoizing `adr.Store` path, if the decorator wraps the store), `cmd/**` and `plugins/mindspec/**` (unchanged, but the domain root), plus the two paths this spec **newly claims** in `.mindspec/domains/workflow/OWNERSHIP.yaml`: `internal/trace/**` (the NDJSON tracer) and `.golangci.yml` (repo lint config).
- **context-system** — `internal/contextpack/**`: `budgeter.go`'s `parseCitedADRs` and the `extractPlanBeadSection` frontmatter-strip migrate onto `internal/frontmatter.Parse` (read-only migration; no change to deterministic budgeting output).

Attribution notes (ADR-0036):
- `internal/frontmatter/**` is a low-level shared package claimed by **no** `OWNERSHIP.yaml`. This spec consumes it read-only (imports `frontmatter.Parse`/`frontmatter.Field`); it is **NOT modified**, so it never enters a bead diff and never trips `adr-divergence-unowned`. All new frontmatter helpers live in owned packages (the mutate helper in `internal/approve`; readers call `frontmatter` directly).
- The trace-ownership decision is **workflow**, chosen honestly from the import graph rather than the report's prose: `internal/trace` is imported by `cmd/mindspec/root.go` + `cmd/mindspec/trace.go` (the `mindspec trace` subcommand and tracer init — `cmd/**` is entirely workflow-owned), `internal/instruct/run.go` (workflow), and `internal/bead/bdcli.go` (execution, a *consumer* that emits events). Two of the three importers and the entire owner-facing CLI surface are workflow; `execution`'s own overview explicitly disclaims CLI infrastructure and only owns git/process operations reached through the `Executor` interface, which trace is not. The review's "used by recording/otel" characterization is factually wrong (verified: `internal/recording` imports neither `internal/trace` nor `internal/otel`, and `internal/otel` does not import `internal/trace`), so nothing pulls trace toward core/observability. Workflow already owns the trace command; it should own the package behind it.
- `.golangci.yml` is repo lint tooling; workflow already owns the sibling repo-tooling paths (`scripts/bd-jsonl-merge-driver.sh`, `.claude/skills/**`, `cmd/**`, `plugins/mindspec/**`), so a one-line `paths:` claim there is the natural, minimal fix — chosen over extending `isDocFile`/the divergence classifier (which would be a broader semantic change to `internal/validate` source).

## ADR Touchpoints

- [ADR-0036](../../adr/ADR-0036-ownership-discovery.md): Ownership Discovery — Zero Framework Cognition for OWNERSHIP.yaml and source_globs (Status: Accepted; Domain(s): workflow, validation, doc-sync, ownership — intersects **workflow**). Governs work area 1 (the `internal/trace/**` and `.golangci.yml` claims), work area 3 Perf #1 (hoisting the per-`(file × domain)` `OWNERSHIP.yaml` loads that this ADR's `attributeDomain`/`LoadOwnership(AtRef)` attribution rules drive), and work area 4 (the doctor `manifestResolvesAny` glob resolution). The caching must keep attribution results identical to the current per-domain-load behavior this ADR specifies. (ADR-0036 supersedes ADR-0031's fallback semantics in part and is the current source of truth for the ownership mechanism, which is why it — not ADR-0031 — is the cited constraint.)
- [ADR-0032](../../adr/ADR-0032-adr-semantic-gates.md): Semantic ADR Coverage Gates with Override and Supersede Flags (Status: Accepted; Domain(s): validation, adr, lifecycle, workflow — intersects **workflow**). Governs work area 3 Perf #4 (memoizing ADR parsing inside `coverageOf`/`checkADRCoverage`/`hasAcceptedCitation`/`checkADRCitations`): the memoization must not change any coverage/relevance/supersede decision this ADR defines — same errors, same warnings, byte-identical outcomes with fewer reads.
- [ADR-0033](../../adr/ADR-0033-tokenizer-interface.md): Pluggable Tokenizer Interface and Deterministic Context Pack Budgeting (Status: Accepted; Domain(s): context-system — intersects **context-system**). Governs the `context-system` work in work area 2: migrating `contextpack/budgeter.go`'s frontmatter scans onto `frontmatter.Parse` must not perturb the deterministic budgeting path (`BuildBead`) or the tokenizer interface this ADR fixes; the change is confined to how frontmatter fences are located.

## Requirements

1. **Ownership claims land first.** Add `internal/trace/**` and `.golangci.yml` to the `paths:` list of `.mindspec/domains/workflow/OWNERSHIP.yaml`. This claim must be present **in the same diff as, or an earlier bead than,** any edit to source under `internal/trace/**` or to `.golangci.yml` — divergence reads each domain's `OWNERSHIP.yaml` at the diffed ref, so the claim only needs to be visible at that ref (same-diff-or-earlier is the exact invariant). Once it is, `validate.attributeDomain` returns `"workflow"` for `internal/trace/event.go` and for `.golangci.yml`, so neither trips `adr-divergence-unowned` when edited. Because `.mindspec/domains/workflow/OWNERSHIP.yaml` is itself workflow-owned non-doc source, the bead that edits it must also touch a workflow domain doc in the same diff (gate-forwardness; see R10).

2. **Delete `trace.Event.MarshalJSON`.** Remove the aliased no-op marshaler at `internal/trace/event.go:52-56`. It calls `json.Marshal(Alias(e))` where `Alias` is a defined type identical to `Event`, so it is byte-identical to Go's default struct marshaling and exists only as dead surface. After removal, `json.Marshal(trace.Event{...})` produces the same NDJSON bytes it does today for a representative populated and a zero-value event (proven by a golden test), and the tracer's emitted lines are unchanged.

3. **Remove the two verifiably-inert `.golangci.yml` `unparam` carve-outs.** Delete the two exclusion entries whose targets no longer exist on this branch: `internal/brownfield/plan.go` (`buildMigrationPlan` — package absent, verified) and `internal/contextpack/builder.go` (`isNeighbor` — function absent, verified). Both patterns match nothing, so removing them is inert and `golangci-lint run ./...` stays clean. Keep the live `internal/validate/state.go` (`validateReviewMode`) carve-out. The third stale carve-out — `internal/next/beads.go` (`findRoot`) — is deliberately **NOT removed in this wave** (see Out of Scope): on this branch's base `findRoot` still exists (wave-1 spec 107's deletion is not yet merged) and `unparam` actively flags it (`golangci-lint run --enable=unparam ./internal/next/...` → `beads.go:53: findRoot - result 1 (error) is always nil`), so the carve-out is live and removing it now would fail the lint gate.

4. **Consolidate the two approve mutate-rewrite scanners.** Collapse `internal/approve/plan.go`'s `updatePlanApproval` (:207-268) and `writeBeadIDsToFrontmatter` (:686-745) — near-identical read → find-`---`-fences → parse-block-to-map → mutate → re-marshal → splice → write flows differing only in the mutation — into one helper in `internal/approve` built on `internal/frontmatter.Parse` (locate the block via `Parse`, `yaml.Unmarshal` it into `map[string]interface{}`, apply a caller-supplied mutation, `yaml.Marshal`, and splice the re-marshaled block ahead of the exact post-fence body bytes). `internal/frontmatter` is NOT modified. For well-formed inputs the helper writes byte-identical files to today's two functions (proven by golden tests for both the approval-fields write and the `bead_ids` write).

5. **Migrate the reader fence-scans onto the canonical package.** Replace the hand-rolled `---` fence scans in `internal/approve/spec.go` (`splitFrontmatter`, :270-288), `internal/approve/impl.go` (`readPlanBeadIDs`, :440-466), `internal/contextpack/budgeter.go` (`parseCitedADRs` :581-608 and the `extractPlanBeadSection` frontmatter-strip :614-631), and `internal/validate/plan.go` (`parsePlanFrontmatter`, :266-303) with calls to `internal/frontmatter.Parse`/`frontmatter.Field`. The known fence-strictness difference (the copies' `strings.TrimSpace` vs the canonical `strings.TrimRight(line, "\r\n")`) is resolved **to the canonical `Parse` semantics** — a deliberate tightening: a space-padded fence line (e.g. `  ---  `) is now treated as no-frontmatter everywhere, matching `frontmatter.Parse`. Redundant manual `#`-comment-line filtering is dropped (YAML's parser already ignores comments). A test demonstrates the tightening (a space-padded fence now yields no-frontmatter through the migrated path).

6. **One approval-status source of truth.** Delete `internal/validate/state.go`'s `readSpecApprovalStatus` (:189-215) prose-scan of the `## Approval` `**Status**:` line and route its two callers (`state.go:70` and `state.go:111`, currently comparing `== "APPROVED"` / `!= "APPROVED"`) through the frontmatter-based `validate.SpecStatus`, comparing case-insensitively to `"Approved"`. Update the existing test at `internal/validate/state_test.go:200`. After the change, the declared spec status comes only from the YAML frontmatter `status:` field; a test proves that when the frontmatter `status:` and the `## Approval` prose disagree, the frontmatter value decides (the ZFC correctness fix).

7. **Hoist OWNERSHIP.yaml loading in validate (Perf #1).** In `internal/validate`, load each candidate domain's `Ownership` once per `ValidateDivergence` (`divergence.go`), `checkInternalPackages` (`docsync.go:459`), and `normalizeImpactedDomains` (`ownership_resolve.go:72`) call into an in-memory map keyed by domain, and attribute changed files against that map instead of calling `loadOwnershipForRef` per `(file × domain)` — the pattern `checkUnclaimedSource` (`docsync.go:220-230`) already uses. This eliminates the per-file re-parse and the up-to-three `git show` subprocesses per domain in `LoadOwnershipAtRef`. Attribution results (owning domain, `Ownership`, and every emitted error/finding) are identical to today. The number of manifest loads per gate run becomes a function of domain count only, independent of the number of changed files (proven by a seam-counting test).

8. **Memoize ADR parsing per validation run (Perf #4).** Ensure the cited ADR set is parsed at most once per validation run — either by pre-parsing the cited IDs into a map or by wrapping `adr.Store` in a memoizing decorator (living in `internal/validate` so `internal/adr` need not change) — so `coverageOf`, `hasAcceptedCitation`, and `checkADRCitations` stop re-reading each ADR file `O(domains × citations)` times. Every coverage, relevance (`adr-cite-irrelevant`), and supersede-chain decision, and every emitted error/warning, is byte-identical to today. Each distinct cited ADR is read from disk at most once per run (proven by a counting-`Store` test).

9. **Doctor walks the tree once (Perf #5).** Refactor `internal/doctor/ownership.go` so the dead-manifest check enumerates the live workspace file list once per ownership check (a single `filepath.WalkDir(root)` honoring the existing `walkExclusions` for `.git/`, `.worktrees/`, `.beads/`) and tests each domain's `paths:` globs against that cached list, replacing the per-domain `manifestResolvesAny` full walk. Warn/pass results are identical to today for populated, empty-stub, and dead-manifest domains. The tree is walked exactly once per check regardless of domain count (proven by a walk-counting seam test).

10. **No regressions; gate-forwardness inherited by planning.** `go test ./...` passes with all existing tests green plus the new tests named in the Acceptance Criteria; `mindspec validate spec 108-cleanup-frontmatter-perf-wave2` reports only the `lifecycle-binding` warning. Planning MUST honor two ordering/attribution constraints baked in here: (a) work area 1's `OWNERSHIP.yaml` claims are present **in the same diff as, or an earlier bead than,** any edit to `internal/trace/**` or `.golangci.yml` — divergence reads each domain's `OWNERSHIP.yaml` at the diffed ref, so the claim only needs to be visible at that ref (same-diff-or-earlier is the exact invariant) — and therefore no bead diff ever contains an unclaimed non-doc path (`adr-divergence-unowned`); and (b) every bead's diff touches a domain doc for **each** domain whose non-doc source it changes (a workflow doc when it changes workflow source, a context-system doc when it changes `internal/contextpack`), because divergence attributes every changed non-doc file and requires the owning domain's docs to move with it.

## Scope

### In Scope
- `.mindspec/domains/workflow/OWNERSHIP.yaml`: add `internal/trace/**` and `.golangci.yml` claims (R1).
- `internal/trace/event.go`: delete `Event.MarshalJSON` (R2).
- `.golangci.yml`: remove the two verifiably-inert `unparam` carve-outs — `brownfield/plan.go` and contextpack `isNeighbor` (R3); the `findRoot` carve-out stays (Out of Scope).
- `internal/approve/{plan.go,spec.go,impl.go}`: consolidate the two mutate-rewrite scanners into one `frontmatter.Parse`-based helper and migrate `splitFrontmatter`/`readPlanBeadIDs` (R4, R5).
- `internal/contextpack/budgeter.go`: migrate `parseCitedADRs` and the `extractPlanBeadSection` strip onto `frontmatter.Parse` (R5).
- `internal/validate/{plan.go,state.go}`: migrate `parsePlanFrontmatter`; delete `readSpecApprovalStatus` and route callers through `validate.SpecStatus` (R5, R6).
- `internal/validate/{ownership.go,divergence.go,docsync.go,ownership_resolve.go}`: hoist per-domain `OWNERSHIP.yaml` loading; memoize per-run ADR parsing (R7, R8).
- `internal/doctor/ownership.go`: walk the repo tree once per ownership check (R9).
- New/updated tests in `internal/{trace,approve,validate,doctor}` for every acceptance criterion, and the workflow domain-doc touches required by gate-forwardness (R10).

### Out of Scope
- **All wave-2 report items not in the four work areas above**, deliberately excluded to keep this wave bounded: setup override-flag validation dedup (DRY #2), TOML-parser consolidation (DRY #4), the shared markdown `## section` helper (DRY #6), the `bd show … [0]` `ShowOne` helper (DRY #7), the OTEL env-var key-map dedup (DRY #8), the `git status --porcelain` tokenizer share (DRY #9), remaining `cmd/mindspec` wiring dedup (DRY #10), harness internal refactors (DRY #11), and the low-impact batch (DRY #12–18).
- **Wave-2 perf items outside work areas 3–4**: the `complete` children/epic fan-out (Perf #2 + #3 — landed by wave-1 spec 107), `migrate`'s per-file `git check-ignore` batching (Perf #6), and the misc prealloc/analyzer items (Perf #7).
- **The `internal/next/beads.go` (`findRoot`) `unparam` carve-out removal.** Deferred until wave-1 spec 107's `findRoot` deletion is in this branch's base (post-107 merge). On the current base `findRoot` still exists and `unparam` flags it (`beads.go:53: findRoot - result 1 (error) is always nil`), so the carve-out is live and removing it now would fail the lint gate. Land it as a one-line rider on whichever of 107/108 merges last, once the base no longer contains `findRoot`.
- **Modifying `internal/frontmatter`.** It is unowned; this spec consumes it read-only. Any new shared helper lives in an owned package.
- **Claiming or editing any unowned path other than `internal/trace/**` and `.golangci.yml`.** `internal/otel` and `internal/frontmatter` remain unclaimed and untouched here.
- Behavioral changes beyond the two documented ones (the frontmatter fence-strictness tightening in R5 and the approval-status source-of-truth correction in R6). No user-facing command output, flag surface, or file format changes.

## Non-Goals

- This spec does not redesign the frontmatter, ownership, ADR, or doctor subsystems; it removes duplication and redundant I/O within their existing contracts.
- It does not add new public API beyond the minimum needed for the consolidations (one approve-local mutate helper; an optional memoizing `adr.Store` decorator internal to `validate`).
- It does not change which ADRs, domains, or gates exist — only how many times files are read while enforcing them.

## Acceptance Criteria

- [ ] `grep -Eq '^[[:space:]]*-[[:space:]]+internal/trace/\*\*' .mindspec/domains/workflow/OWNERSHIP.yaml && grep -Eq '^[[:space:]]*-[[:space:]]+\.golangci\.yml' .mindspec/domains/workflow/OWNERSHIP.yaml` (both claims present; exit 0).
- [ ] `go test ./internal/validate/ -v -run 'TestWorkflowOwnsTraceAndGolangci$' | grep -q -- '--- PASS: TestWorkflowOwnsTraceAndGolangci'` (a test asserting `attributeDomain` returns `"workflow"` for `internal/trace/event.go` and `.golangci.yml`).
- [ ] `! grep -q 'func (e Event) MarshalJSON' internal/trace/event.go` (the dead marshaler is gone; exit 0).
- [ ] `go test ./internal/trace/ -v -run 'TestEventNDJSONGolden$' | grep -q -- '--- PASS: TestEventNDJSONGolden'` (NDJSON bytes for a populated and a zero-value event unchanged after removal).
- [ ] `for p in 'brownfield/plan' 'isNeighbor'; do grep -q "$p" .golangci.yml && exit 1; done; exit 0` (the two inert carve-outs removed) and `grep -q 'findRoot' .golangci.yml` (the live `findRoot` carve-out and the `validateReviewMode` carve-out remain untouched; exit 0).
- [ ] `golangci-lint run ./...` exits 0 (lint clean after carve-out removal).
- [ ] `go test ./internal/approve/ -v -run 'TestApprovalWriteByteIdentical$' | grep -q -- '--- PASS: TestApprovalWriteByteIdentical'` and `go test ./internal/approve/ -v -run 'TestBeadIDsWriteByteIdentical$' | grep -q -- '--- PASS: TestBeadIDsWriteByteIdentical'` (both approve writes produce byte-identical files for well-formed inputs through the shared helper).
- [ ] `go test ./internal/validate/ -v -run 'TestPlanFrontmatterFenceStrictnessTightened$' | grep -q -- '--- PASS: TestPlanFrontmatterFenceStrictnessTightened'` (a space-padded `---` fence is now treated as no-frontmatter via the migrated path, matching `frontmatter.Parse`).
- [ ] `! grep -q 'func readSpecApprovalStatus' internal/validate/state.go` and `go test ./internal/validate/ -v -run 'TestSpecApprovalStatusFromFrontmatter$' | grep -q -- '--- PASS: TestSpecApprovalStatusFromFrontmatter'` (prose-scan deleted; a test proves the YAML `status:` field decides when it disagrees with the `## Approval` prose).
- [ ] `go test ./internal/validate/ -v -run 'TestDivergenceOwnershipLoadedPerDomainNotPerFile$' | grep -q -- '--- PASS: TestDivergenceOwnershipLoadedPerDomainNotPerFile'` (a seam-counting test: manifest-load count over a multi-file diff is a function of domain count, not changed-file count).
- [ ] `go test ./internal/validate/ -v -run 'TestADRParsedOncePerValidationRun$' | grep -q -- '--- PASS: TestADRParsedOncePerValidationRun'` (a counting `adr.Store` test: each distinct cited ADR is read at most once per run, with identical coverage/relevance outcomes).
- [ ] `go test ./internal/doctor/ -v -run 'TestOwnershipCheckWalksTreeOnce$' | grep -q -- '--- PASS: TestOwnershipCheckWalksTreeOnce'` (a walk-counting seam test: the workspace tree is walked exactly once per ownership check regardless of domain count).
- [ ] `go test ./...` exits 0 (full suite green, including all new tests).
- [ ] `mindspec validate spec 108-cleanup-frontmatter-perf-wave2` exits 0 with only the `lifecycle-binding` warning and no errors.

## Validation Proofs

- `go test ./internal/trace/ ./internal/approve/ ./internal/validate/ ./internal/doctor/ ./internal/contextpack/`: all package suites green, including the named new tests above.
- `go test ./...`: full suite green.
- `golangci-lint run ./...`: clean (proves R3's carve-out removal introduced no findings).
- `grep -Eq '^[[:space:]]*-[[:space:]]+internal/trace/\*\*' .mindspec/domains/workflow/OWNERSHIP.yaml`: the trace claim is present (exit 0).
- `mindspec validate spec 108-cleanup-frontmatter-perf-wave2`: only the `lifecycle-binding` warning (the expected pre-approve state).
- Manual/agent read of the diff confirms `internal/frontmatter` is unmodified and each bead touching non-doc source in a domain also touches that domain's docs (gate-forwardness), so `mindspec complete` divergence stays green.

## Open Questions

_All open questions are resolved; the resolutions are folded into the requirements, scope, and ADR touchpoints above._

- **RESOLVED — which domain owns `internal/trace`.** Workflow. The import graph (two of three importers are workflow: `cmd/mindspec` and `internal/instruct`; the third, `internal/bead`, is an event-emitting consumer), the fact that the `mindspec trace` subcommand and tracer init already live in workflow-owned `cmd/**`, `execution`'s explicit disclaimer of CLI infrastructure, and the verified falsity of the review's "recording/otel" claim all point to workflow. (R1, Impacted Domains.)
- **RESOLVED — `.golangci.yml` handling.** Claim it in the workflow `OWNERSHIP.yaml` rather than extending the divergence/`isDocFile` classifier: workflow already owns the sibling repo-tooling paths, the claim is a one-line change, and it avoids a broader semantic change to `internal/validate`. (R1, R3.)
- **RESOLVED — ADR citation set.** `{ADR-0036, ADR-0032, ADR-0033}` — each Accepted and each intersecting an impacted-domain token (0036/0032 → workflow, 0033 → context-system). ADR-0031, though topically the doc-sync gate, is **not** cited: its `Domain(s)` (validation, doc-sync, lifecycle, ownership) intersect neither `workflow` nor `context-system`, so citing it would trip `adr-cite-irrelevant`; ADR-0036 (its partial successor for the ownership mechanism) is the correct, intersecting constraint. (ADR Touchpoints.)
- **RESOLVED — frontmatter fence-strictness conflict.** Resolved to the canonical `frontmatter.Parse` semantics (`TrimRight(line, "\r\n")`), a deliberate tightening called out in R5 and proven by a test; the redundant manual `#`-comment filter is dropped because YAML already ignores comments.
- **RESOLVED — approval-status source of truth.** The YAML frontmatter `status:` field (via `validate.SpecStatus`), replacing the `## Approval` prose scan; documented as the ZFC correctness fix in R6.

## Approval

- **Status**: DRAFT
- **Approved By**: -
- **Approval Date**: -
- **Notes**: -
