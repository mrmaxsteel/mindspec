# spec-115-final — Final Review (12 reviewers, four families) — whole spec branch vs main

**Worktree**: `/Users/Max/replit/mindspec/.worktrees/worktree-spec-115-impl-approve-panel-gate`
**Branch**: `spec/115-impl-approve-panel-gate` @ **21374bb8** (all 3 beads merged). **Base**: `origin/main` @ `f02a3a49`. Whole-spec diff = `git diff origin/main..HEAD`.
**Panel**: 12 slots — **F1–F3 Fable-sub (Opus/Sonnet — Fable quota-walled)**, O1–O3 Opus, S1–S3 Sonnet, G1–G3 codex. **Pass = ≥11 APPROVE, no REJECT.** Findings never out-voted (a confirmed finding is fixed or evidence-refuted, even at 11/12).

**READ-ONLY**: verdict JSON only; ALL scratch ABSOLUTE `/tmp`; NEVER edit the worktree; leave `git status --porcelain` clean.

## What spec 115 delivers (the whole thing, now on one branch)
Closes the last un-gated bead-branch merge path in the binary: `mindspec impl approve` gains a pre-terminal REFUSAL gate so it cannot finalize a spec while any closed bead under its epic lacks proof of panel settlement — the `mindspec-o4fd` P1 hole (a `bd close`'d RC-bearing bead riding an `impl approve` auto-merge un-gated). Three beads, all shipped via 8/8 unanimous bead panels:
- **Bead 1 — detection substrate** (`internal/lifecycle`, `internal/complete`, `internal/executor`): `ScanOrphanedClosedBeads` error-preserving core + `ClosedEpicBeadIDs`; `CheckPendingObligations` check-only obligation predicate (fail-closed, hardened over 4 codex rounds to exhaustively reject present-null/corrupt/shape-invalid obligation metadata) + `PanelGateRoots`; executor comment-only fix (AC8); `TestFinalizeEpic_MergesOnlyWorktreeRealBranches` (AC12, Fact 2).
- **Bead 2 — the impl-approve gate** (`internal/approve/impl.go` `runOrphanObligationGate`): four legs — R1 orphan scan (fail-closed), Option-B worktree-enumeration merge-prevention leg (AC13, closes the round-6 TOCTOU), R2 advisory slot naming (decoration only), R3 durable-obligation backstop (fail-closed on data + plan enumeration); the branch-existence probe stays fail-OPEN (never false-refuses a deleted branch, AC2d); 3 new acyclic import edges (approve→lifecycle/complete/gitutil); AC11 (Fact 1: MergeBase pre-scan abort); AC1-7/11/13 + AC4 AST call-order anchor. Also closes a pre-existing hole (a missing plan.md WITH commits previously merged un-gated).
- **Bead 3 — docs + ownership**: ADR-0037 §1 dated 2026-07-11 amendment (contract reach = every merging lifecycle verb; refusal scoped to the DETECTABLE guarantee; single settlement surface = `mindspec complete`; hatches unaffected; residual disclosed); ms-impl-approve skill refusal section in all 3 surfaces (AC9); `TestLifecycleOwnershipExactlyOneWorkflowClaimant` using production `validate.GlobMatch` overlap matching (AC10).

**Load-bearing design invariants (verify the WHOLE branch honors them):**
- **C+B (no exit-code classifier):** detection uses the unchanged `gitutil.BranchExists` bool; NO `BranchExistsE`, NO `show-ref` Go code anywhere (0-hit); `internal/gitutil` byte-unchanged (`git diff origin/main -- internal/gitutil/` empty). Fail-closed safety is STRUCTURAL: Fact 1 (`MergeBase` pre-scan abort, AC11) + Fact 2 (`FinalizeEpic` merges only worktree-enumerated real branches, AC12).
- **Option B (TOCTOU fix):** the gate enumerates via `bead.WorktreeList()` — the SAME source `FinalizeEpic` merges from — so a transient probe miss can't desync detection from merge (AC13). Added to, not replacing, the shared `FindOrphanedClosedBeads` predicate.
- **Single settlement home:** the gate REFUSES but never computes Allow/Block, never writes panel metadata, never applies a refutation; recovery routes through `mindspec complete`. ADR-0030-clean (no executor seam; no executor git-I/O in approve). Fail directions: all detection legs fail-CLOSED; branch-existence bool fail-OPEN by design; advisory degrades to omission.
- **Disclosed residuals (must stay honestly disclosed):** (a) a raw-`git merge`d bead with no durable obligation is undetectable; (b) cross-spec orphans on the global merge loop = `mindspec-blp6`; (c) the two-List() concurrent-mutation window (lifecycle-serialization bound).

## What to verify at `21374bb8` (whole spec)
1. **Every spec AC (AC1–AC13) is present, homed, and RED-on-revert on the merged branch.** Cross-walk the spec's Acceptance Criteria against the shipped tests across all 3 beads; each named test exists (`grep -q 'func Test…'`) and its exact `-run` passes; the discriminators are genuinely RED-on-revert (the spec's discriminator SHAs). AC8 diff-grep, AC11/AC12 real-git, AC13 worktree-enum, AC9 doc greps, AC10 ownership overlap — all hold.
2. **The gate delivers the fail-closed guarantee end-to-end, with NO remaining un-gated-merge path** (ordinary flow, no raw tamper) beyond the three DISCLOSED residuals. All four legs fail-CLOSED where required; the branch-existence bool never false-refuses a deleted branch; the obligation predicate exhaustively rejects corrupt metadata; the advisory can't un-refuse; hatches bypass nothing.
3. **No cross-bead regression / whole-tree green.** `go build ./...`; `go test ./...` (or at least all touched packages + their consumers: `./internal/approve ./internal/complete ./internal/lifecycle ./internal/executor ./internal/setup ./internal/doctor ./cmd/mindspec`); `go vet ./...`; `gofmt -l ./cmd ./internal` empty; **`golangci-lint run ./...`** (CI runs it — misspell has bitten prior specs); `mindspec validate spec 115-impl-approve-panel-gate`. The 3 new import edges are acyclic; existing tests pass with no semantic edits beyond the sanctioned Bead-2 fixture accommodations (documented in `reviews/spec-115-bead2/consolidated-round-1.md`).
4. **ADR/skill/contract docs are accurate + non-overclaiming** vs the shipped gate (the Bead-3 round-2 scoping); the residuals are honestly disclosed; ADR-0037/0030/0035/0023 fitness holds; no design contradiction (C+B / Option B / single-settlement-home).
5. **CI-readiness / merge-readiness.** The whole-spec diff would pass the CI gates (build, vet, gofmt, golangci-lint incl. misspell). The `--allow-doc-skew` disposition is legitimate (repo has no `.mindspec/docs/domains/`; contract docs live in ADR-0037 + skill). No stray fence violations (raw `git merge`, `--no-verify`, `MINDSPEC_ALLOW_MAIN`) in the production diff.

## Per-slot lens
- **F1 Fable-sub** falsifiability (all AC1-13 RED-on-revert across the branch) · **F2 Fable-sub** grounding (all spec.md + ADR citations resolve on the merged tree) · **F3 Fable-sub** contradiction/completeness (spec design coherent end-to-end; residuals disclosed; OQs resolved).
- **O1 Opus** anti-gaming / SECURITY completeness (the gate actually delivers the fail-closed guarantee; hunt any un-gated path beyond the 3 disclosed residuals) · **O2 Opus** ADR / import-edge / blast-radius (3 edges acyclic; ADR-0037/0030 fitness; shared predicate byte-unchanged for complete/next/doctor; no cross-bead regression) · **O3 Opus** provenance / decomposition (every spec AC homed with a proof across the 3 beads; the Bead-2 fixture-deviation is sanctioned).
- **S1 Sonnet** empirical FULL-TREE (build / `go test` all touched pkgs + consumers / vet / gofmt / **golangci-lint run ./...** / validate — actual output) · **S2 Sonnet** no-regression (whole existing suite green; no behavior change to unrelated code; the sanctioned Bead-2 fixture changes don't mask a regression) · **S3 Sonnet** CI-readiness (the PR would pass CI: golangci-lint/misspell/gofmt/vet; the doc-skew disposition legit).
- **G1 codex** adversarial SECURITY (whole-gate: any remaining realizable un-gated-merge path, any fail-open, beyond the 3 disclosed residuals) · **G2 codex** fail-closed completeness (the obligation predicate + all four legs exhaustively fail-closed; no corrupt-metadata bypass; the Option-B leg genuinely closes the TOCTOU) · **G3 codex** whole-tree empirical + integration (build/test/vet/golangci-lint on the branch; the 3 beads integrate with no seam mismatch; the exported seams consumed correctly).

Verdict: APPROVE / REQUEST_CHANGES / REJECT. Output JSON to `<this-dir>/<slot>-round-1.json`: `reviewer_id`, `verdict`, `confidence`, `rationale` (≤200 words), `concrete_changes_required` (empty if APPROVE), `findings`. An artifact/measurement/CI-gate finding may set `"hard_block": true`.
