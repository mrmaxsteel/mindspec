# spec-115-bead1 — Round 4 Bead Panel (8 reviewers)

**Bead worktree**: `/Users/Max/replit/mindspec/.worktrees/worktree-spec-115-impl-approve-panel-gate/.worktrees/worktree-mindspec-fgmg.1`
**Branch**: `bead/mindspec-fgmg.1` @ **a8da895a** (impl `75e82b52` + r1/r2/r3 fixes). **Pass = 8/8 UNANIMOUS.** Findings never out-voted.

**READ-ONLY**: verdict JSON only; ABSOLUTE `/tmp` scratch; NEVER edit; leave `git status --porcelain` clean.

## History → the round-4 fix (delta `git diff e71a7991..a8da895a`, only `internal/complete/panel_advisory.go` + test)
The obligation predicate `uncoveredPendingObligations` has been hardened over 3 rounds of codex adversarial review, all in the corrupt-shape/fail-closed class:
- R1: present-null `refutation_pending_entries` → now errors (comma-ok).
- R2: `panel_refuted_entries` corrupt/null when pending empty → now validated before the early return (structural reorder).
- **R4 (this round): the element-level asymmetry.** Pending-array elements were shape-checked (empty slot / round<1 → error); refuted-array elements were NOT (a `null` element decodes to a zero-valued `panel.Refutation`). The fix adds a loop validating EVERY decoded `panel_refuted_entries` element's shape (`Slot=="" || Round<1` → error), placed after `decodeRefutations` and BEFORE the `len(pending)==0` early return — the SAME rule already applied to pending. Now **every field of every element of BOTH arrays is validated before any early return** (exhaustive, symmetric). Note: R8 acknowledged this specific case is inert (a malformed covering entry cannot key-match a valid pending obligation); the fix closes it for exhaustive fail-closed completeness + pending/refuted symmetry + the spec's unconditional present-but-corrupt-REFUSES contract. `TestPendingObligationPredicate` now 20 subtests (added: `refuted:[null]`→error; empty-slot refuted→error; round<1 refuted→error; + a valid-covered no-false-refuse pin). Fixer RED-on-revert-checked (removed the loop → the 3 new subtests failed → restored).

## What to verify at `a8da895a`
1. **The corrupt-shape class is now EXHAUSTIVELY closed (R8's lens — the decisive check).** Trace EVERY control path through `uncoveredPendingObligations`: both top-level keys are present-null + decode validated up front; every element of BOTH the pending and refuted arrays is now shape-validated (empty slot / round<1 → error) before any early return. Confirm there is NO remaining realizable metadata shape (present-null, wrong-typed, null element, missing/zero slot or round, in EITHER key) that decodes-as-empty / passes fail-open. If validation is exhaustive (no unvalidated field remains), the class is closed → APPROVE. If a genuinely NEW class (not corrupt-shape) or a real remaining corrupt-shape edge exists, REQUEST_CHANGES with the exact realizable input.
2. **NO false-refuse.** `{}` (both absent) → nil; `{pending:[], refuted:[]}` (valid empties, no elements to validate) → nil; a valid non-empty pending fully covered by a VALID covering entry (non-empty slot, round≥1) → nil (coverage unchanged). The new refuted-element loop must not reject any VALID covering entry.
3. **NO regression.** `uncoveredPendingObligations` still the single shared core; `reconcilePendingRefutations` calls it and its behavior on VALID inputs is byte-identical — a valid bead's settle path is unchanged (the `TestPanelRefuted_*` suite passes unchanged). The new refuted-element validation only fires on malformed covering entries, which valid beads never have. No existing test expectation edited.
4. **Scope + fences + green.** Delta touches ONLY `internal/complete/panel_advisory.go` + test (purely additive). No approve/gitutil/docs; `git diff main -- internal/gitutil/` empty; `BranchExistsE`/`show-ref` 0-hit; `go build ./...` + `go test -count=1 ./internal/lifecycle ./internal/complete ./internal/executor` green; gofmt/vet/golangci-lint clean.
5. **Everything from rounds 1-3 still holds** — your prior APPROVE lens (author faithfulness, seam signatures, the four named substrate tests RED-on-revert, AC12(a) deviation, byte-identity for consumers, scope/grounding) is undisturbed by a fix confined to the obligation core's validation.

## Per-slot lens
- **R1 Opus** author/scope · **R2 Opus** fail-closed completeness + no over-correction · **R3 Opus** RED-on-revert of the 3+1 new subtests · **R4 Sonnet** empirical (build/tests/20-subtests/fences/gofmt/vet/lint) · **R5 Sonnet** seam/type (signatures + shared core intact) · **R6 Sonnet** no-regression (reconcile + consumers unchanged) · **R7 Fable** scope/grounding · **R8 codex** adversarial — the DECISIVE slot: verify the corrupt-shape class is now EXHAUSTIVELY closed (no round-5 sibling) and no false-refuse introduced. If exhaustive, APPROVE.

Verdict: APPROVE / REQUEST_CHANGES / REJECT. Output JSON to `<this-dir>/<slot>-round-4.json`: `reviewer_id`, `verdict`, `confidence`, `rationale` (≤200 words), `concrete_changes_required` (empty if APPROVE), `findings`.
