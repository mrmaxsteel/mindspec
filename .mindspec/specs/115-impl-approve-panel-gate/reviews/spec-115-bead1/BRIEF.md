# spec-115-bead1 — Round 1 Bead Panel (8 reviewers, four families)

**Bead worktree**: `/Users/Max/replit/mindspec/.worktrees/worktree-spec-115-impl-approve-panel-gate/.worktrees/worktree-mindspec-fgmg.1`
**Branch**: `bead/mindspec-fgmg.1` @ **75e82b52** (single commit `feat(115): Bead 1 — detection substrate + structural pins`). Base = plan-approve `5d0a6e4b`. Diff under review = `git diff 5d0a6e4b..75e82b52`.
**Panel**: 8 slots — R1–R3 Opus, R4–R6 Sonnet, R7 Fable, R8 codex (real gpt-5.6-sol). **Pass = 8/8 UNANIMOUS APPROVE, no dissent.** Every REQUEST_CHANGES is a finding to fix or evidence-refute; the panel passes only when all 8 APPROVE. Findings never out-voted.

**READ-ONLY**: write your verdict JSON only; ALL scratch under ABSOLUTE `/tmp` (or `t.TempDir()` in Go); NEVER edit the worktree; leave `git status --porcelain` clean (the worktree MUST be clean at `mindspec complete` time).

## What Bead 1 does (the detection substrate for spec 115's impl-approve gate)

Spec 115 makes `mindspec impl approve` refuse to finalize a spec while any closed bead under its epic lacks proof of panel settlement. Bead 1 lands the **substrate** that Bead 2's gate consumes, plus two structural pins — with **zero behavior change for every existing consumer**. It does NOT add the gate itself (that's Bead 2) and NOT the docs (Bead 3).

Six files changed (752 insertions, 75 deletions):
1. **`internal/lifecycle/orphans.go`** — extracted an exported error-preserving core `ScanOrphanedClosedBeads(specID, workdir, excludeBeadID string) ([]Orphan, error)` that PROPAGATES the three cleanly-signalled infra errors (epic-lookup `findEpicBySpecIDFn`, bd-list `listClosedBeadsFn`, ancestry `isAncestorFn`→`gitutil.IsAncestor`). `FindOrphanedClosedBeads` is now a thin FAIL-OPEN wrapper delegating to the core and swallowing its error — **byte-identical** for its 3 consumers (`complete/complete.go:111`, `cmd/mindspec/next.go:459`, `doctor/orphaned_beads.go`). Also exported `ClosedEpicBeadIDs(specID string) ([]string, error)` wrapping the SAME `findEpicBySpecIDFn`+`listClosedBeadsFn` pair. The branch-existence trigger stays the UNCHANGED bool `branchExistsFn = gitutil.BranchExists` (false = no trigger, by design — C+B, never false-refuses a deleted branch).
2. **`internal/lifecycle/orphans_test.go`** — `TestScanOrphanedClosedBeads_ErrorPreserving` (4 subtests incl. the MIXED-list parity case: ancestry errors for bead A while a later bead B is a provable orphan → core returns the error, wrapper still returns B).
3. **`internal/complete/panel_advisory.go`** — extracted a NEW UNEXPORTED helper `uncoveredPendingObligations` (computes the uncovered (slot,round) set); exported `CheckPendingObligations(beadID string, getMeta func(string)(map[string]interface{},error)) error` (DI metadata reader; fail-CLOSED on read/decode/shape, exact on (slot,round) coverage, nil for no-pending); exported `PanelGateRoots(root, wtPath, specID string) []string` (thin wrapper over `panelGateRoots`); re-expressed `reconcilePendingRefutations` over the SAME unexported helper (it CONSUMES the uncovered set to SETTLE — it does NOT call the erroring predicate).
4. **`internal/complete/panel_advisory_test.go`** — `TestPendingObligationPredicate` (9 subtests) + `TestCompleteRun_RegatesAlreadyClosedOrphan` (AC7(a): already-closed orphan with unresolved RC → `complete.Run` Blocks before the already-closed tolerance; after durable refutation → succeeds "already closed" and merges).
5. **`internal/executor/mindspec_executor.go`** — COMMENT-ONLY fix at `:393-409`: dropped "same regression-only rule as CompleteBead"; the new comment names `guardMergeLayout`'s directional layout-regression scope and disclaims panel/obligation/orphan enforcement (ADR-0030). Executable lines unchanged (AC8).
6. **`internal/executor/finalize_worktree_only_test.go`** (new) — `TestFinalizeEpic_MergesOnlyWorktreeRealBranches` (AC12, Fact 2, 3 real-git subtests: (a) no-worktree branch ref never merged; (b) tag-shadow of a deleted branch not enumerated as `bead/`; (c) dangling symref can't `git worktree add`).

## Implementer-declared deviation (assess explicitly)
The AC12 subtest (a) was adjusted: it initially asserted ancestry against the spec branch, but `FinalizeEpic` legitimately DELETES the spec branch after its own (no-op) direct merge to main — so the assertion now checks against `main` plus the ABSENCE of the orphan's tell-tale file. **Verify this still correctly pins Fact 2** (a no-worktree `bead/<id>` branch ref is never merged) and isn't weakened by the change.

## What to verify at `75e82b52`

1. **Diff faithfully implements plan Bead-1 steps 1–7**, nothing out of scope. Confirm NO change to `internal/approve` (Bead 2), NO docs (Bead 3), and — load-bearing — **NO change to `internal/gitutil`** (`git diff main -- internal/gitutil/` empty) and the negative fence stays 0-hit (`! grep -rn 'BranchExistsE' --include='*.go' .`; `! grep -rn 'show-ref' --include='*.go' internal/`).
2. **Fail-open vs fail-closed directions are exactly right.** The core PROPAGATES the three infra errors; the wrapper SWALLOWS them (complete/next/doctor byte-identical); `CheckPendingObligations` REFUSES (error) on metadata-read error, corrupt entries, or shape-invalid entry (empty slot / round < 1), and is exact on (slot,round) coverage — never decode-as-empty; a no-pending bead is a nil no-op. The branch-existence trigger stays the unchanged bool (false = no trigger).
3. **No behavior change for existing consumers.** `FindOrphanedClosedBeads`'s three call sites unchanged; `reconcilePendingRefutations` behavior preserved (re-expressed over the shared unexported helper, NOT the erroring predicate — the F3-1 clarification); every pre-existing test in the three packages passes with ZERO semantic edits (inspect `git diff` for any changed existing-test expectation).
4. **The four new tests are sound + RED-on-revert.** Each genuinely fails if its production change is reverted (not a tautology, not GREEN-at-base). Run each named `-run` (use `-count=1` to defeat the test cache). The AC8 pair holds (`git diff main … | grep -c '^-.*rule as CompleteBead'` ≥1 AND 0 remaining). AC12's three subtests actually assert Fact 2 (assess the declared deviation). The mixed-list parity subtest actually distinguishes core-error from wrapper-still-returns-B.
5. **Seam signatures match what Bead 2 will consume.** `ScanOrphanedClosedBeads`/`ClosedEpicBeadIDs`/`CheckPendingObligations`/`PanelGateRoots` exactly as the plan pins them (`CheckPendingObligations` takes a DI `getMeta func(string)(map[string]interface{},error)` so `internal/approve` can pass its own `bead.GetMetadata` without reaching into complete's private fns). No hidden dependency; the exports are acyclic for the future `approve→lifecycle`/`approve→complete` edges.
6. **Green + clean.** `go build ./...`; `go test -count=1 ./internal/lifecycle ./internal/complete ./internal/executor`; `go vet`; `golangci-lint run` (CI runs it — check misspell/lint); `gofmt -l ./cmd ./internal` empty; `git status --porcelain` clean.

## Per-slot lens
- **R1 Opus** — author-of-record: diff ↔ plan Bead-1 steps 1–7; every step present; nothing out of scope (no approve/gitutil/docs).
- **R2 Opus** — fail-open/fail-closed correctness: core propagates the 3 infra legs; wrapper swallows; `CheckPendingObligations` fail-closed on read/decode/shape and exact on coverage; AC7(a) regate semantics.
- **R3 Opus** — RED-on-revert discipline: each of the 4 new tests fails on revert; AC8 pair; AC12 subtests (incl. the deviation) truly pin Fact 2; mixed-list parity subtest discriminates.
- **R4 Sonnet** — empirical prober: run build / `go test -count=1` (3 pkgs) / each named `-run` / fences / `git diff main -- internal/gitutil/` / gofmt / vet / golangci-lint; report ACTUAL output.
- **R5 Sonnet** — schema/type/seam correctness: exported signatures match the plan's pinned shapes + Bead 2's needs; DI metadata reader; `uncoveredPendingObligations` is the shared core beneath BOTH the exported predicate and reconcile.
- **R6 Sonnet** — no-regression/consumer byte-identity: `FindOrphanedClosedBeads` consumers unchanged; `reconcilePendingRefutations` preserved; no existing-test expectation edited (`git diff` inspection).
- **R7 Fable** — contradiction/scope/grounding: nothing outside Bead-1 scope; executor change truly comment-only; F3-1 (unexported helper) and F2-2 (no bare `PanelGateRoots` grep) honored; citations resolve.
- **R8 codex** — next-bead integration + adversarial: will Bead 2 consume the four exports cleanly; any hidden coupling; adversarially probe the "byte-identical" and "fail-closed" claims; run the empirical checks independently.

Verdict: APPROVE / REQUEST_CHANGES / REJECT. Output JSON to `<this-dir>/<slot>-round-1.json`: `reviewer_id` ("<slot> <family>"), `verdict`, `confidence`, `rationale` (≤200 words), `concrete_changes_required` (empty if APPROVE), `findings` (per §1–§6, ADDRESSED/PARTIAL/MISSED/NEW_ISSUE where relevant).
