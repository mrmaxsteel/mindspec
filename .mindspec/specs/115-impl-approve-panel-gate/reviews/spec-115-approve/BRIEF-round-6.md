# spec-115-approve — Round 6 Spec-Approval Panel (9 reviewers, three families)

**Worktree**: `/Users/Max/replit/mindspec/.worktrees/worktree-spec-115-impl-approve-panel-gate`
**Branch**: `spec/115-impl-approve-panel-gate` @ **8b07e89f54cb8faecd5491648b3229ccd9213709**. Base = `origin/main` (`f02a3a49`).
**Under review**: `.mindspec/specs/115-impl-approve-panel-gate/spec.md`.
**Panel**: 9 slots — F1–F3 Fable, O1–O3 Opus, **G1–G3 codex**. **Pass = ≥8 APPROVE, no REJECT.** Findings never out-voted.

**READ-ONLY**: verdict JSON only; pin reads to `8b07e89f`; ALL scratch under ABSOLUTE `/tmp`; NEVER edit the worktree; leave `git status` clean.

## What changed since round 5 — the C+B pivot (DO NOT re-litigate the decision; verify its IMPLEMENTATION)
Spec-approval rounds 2→5 chased a per-branch git existence probe that could fail-closed on infra without false-refusing a deleted branch. Round 5 (9/9 RC) PROVED that is impossible: no single git ref-probe distinguishes "genuinely absent" from "ref-store unreadable" by exit code (`--quiet` → both exit 1; no-`--quiet` `show-ref --verify` → both exit 128). A 5-slot DESIGN-DECISION panel (+adversarial Opus sub) then chose **Approach C+B** (recorded in `reviews/spec-115-branch-probe-decision/DECISION-OUTCOME.md`). **The design decision is SETTLED — this round verifies it is correctly + coherently implemented, and that the structural refutation actually holds. Do NOT re-argue A/B/C/D.**

**C+B, as implemented in round 6 (commit `8b07e89f`, spec.md only):**
1. The exit-code existence classifier is DELETED. The branch-EXISTENCE trigger reverts to the plain `gitutil.BranchExists` bool (`gitops.go:94-100`): present→true (candidate orphan); absent-OR-infra→false (so a genuinely-deleted branch NEVER false-refuses; an infra failure degrades to "treated as absent" = fail-OPEN at the probe — which is SAFE per the two structural facts below). `BranchExistsE` and the `internal/gitutil` change are removed (gitutil dropped from Impacted Domains/Scope).
2. The error-preserving core `ScanOrphanedClosedBeads` STILL fail-CLOSES on the THREE cleanly-signalled legs (epic-lookup / bd-list / ancestry — these git/bd commands cleanly return errors; e.g. `git merge-base --is-ancestor` gives 0/1/other-is-error). ONLY the branch-existence leg reverts to the fail-open bool.
3. The branch-probe's absent-vs-infra ambiguity is audited-REFUTED (Non-Goals), backstopped by TWO structural facts, each pinned as a NEW named AC:
   - **Fact 1 (AC11, `TestApproveImpl_UnreadableRefStoreAbortsPreScan`)**: `exec.MergeBase("main", specBranch)` at `impl.go:249` runs BEFORE the orphan scan and fails (exit 128) on a whole-`refs/heads`-unreadable store → ApproveImpl aborts fail-closed before the scan.
   - **Fact 2 (AC12, `TestFinalizeEpic_MergesOnlyWorktreeRealBranches`)**: `FinalizeEpic` (`mindspec_executor.go:381-422`) merges ONLY branches from `WorktreeOps.List()` (real branches, via `HasPrefix(e.Branch,"bead/")` at `:385` + `MergeInto` at `gitops.go:152`). A worktree requires a real branch (tag-only name → detached/unenumerated; dangling symref → worktree-add fails), so a masked/corrupt single bead ref is either unenumerated or its merge fails-closed. No un-gated merge.
4. New Non-Goal: ref-name-ambiguity (tag/symref shadow) — raw-git tamper, additionally unreachable via FinalizeEpic. Plus a blp6 constraint: any future FinalizeEpic merge-loop change MUST preserve the "merge only worktree-enumerated real branches" invariant (pinned by AC12).

## What to verify at `8b07e89f`
1. **C+B is cleanly + coherently implemented** — the classifier is fully deleted (no LIVE `show-ref`/`BranchExistsE`/0-1-128/three-way design references; only historical/negative mentions incl. the negative-pin grep are OK). The three-cleanly-signalled-legs-fail-closed + branch-existence-probe-fail-open split is stated precisely and internally consistent in R1 + OQ2. `internal/gitutil` is dropped from Impacted Domains + Scope. No dangling references.
2. **The structural refutation HOLDS** — verify Facts 1 & 2 against the actual code (`impl.go:249` MergeBase pre-scan; `mindspec_executor.go:381-422`/`:385` worktree-gated merge; `gitops.go:152` MergeInto; `gitops.go:94-100` BranchExists). Does the audited refutation genuinely close the un-gated-merge risk given the fail-open probe? Is there a NON-tamper, ordinary-flow (`bd close`→`impl approve`) path where a masked orphan gets merged that Facts 1&2 miss? (A raw-git-tamper path — tags/symrefs/manual git merge — is already disclaimed by the Non-Goals; not a finding.)
3. **AC11 & AC12 are sound + falsifiable** — both new named tests are genuinely RED at `8b07e89f` (0 code hits — verify), pair a real assertion with a runnable proof, and actually pin Facts 1 & 2 (AC11: unreadable store → abort at/before :249, terminal fns uncalled; AC12: no-worktree ref never merged + tag/symref not enumerated). Do they falsify the right behavior?
4. **No regression to the validated core** — the REFUSE design, R2 advisory, R3 obligation backstop, recovery convergence, epic-scope, ownership→workflow, ADR-0030/0037 touchpoints, and the never-false-refuse property (`TestApproveImpl_DeletedBranchNoRefusal`) are intact. All 4 OQs `[x]`. The shared `FindOrphanedClosedBeads` wrapper stays byte-identical for complete/next/doctor.
5. **Grounding** — every cited file:line exists + means what the spec says at `8b07e89f`.

## Per-slot lens
- **F1** falsifiability (AC11/AC12 + all discriminators genuinely RED); **F2** grounding (Facts 1&2 line cites; classifier-deletion negative pin); **F3** contradiction/scope/OQ coherence (the three-legs vs probe-fail-open split; the refutation vs R1; all OQs).
- **O1** anti-gaming completeness (does the fail-open probe + structural refutation leave ANY ordinary-flow un-gated-merge path? stress Facts 1&2); **O2** ADR/ownership (gitutil dropped correctly; lifecycle→workflow intact; ADR-0030/0037 fit; the blp6 invariant constraint); **O3** decomposition + impacted-domains parity (gitutil removed cleanly; still ~2-3 beads).
- **G1** grounding empirical (verify the Facts 1&2 code + AC11/AC12 tests absent at `8b07e89f`); **G2** refutation soundness (reason about + read the code to judge whether the audited refutation genuinely holds for the ORDINARY flow — is a masked orphan always caught by MergeBase@:249 or FinalizeEpic's worktree-gating? focus on spec-correctness verification, NOT on constructing raw-git exploits, which are out-of-scope tamper); **G3** AC-runnability (AC11/AC12 + every AC1-AC12 proof RED at `8b07e89f`, exact-named runs).

Verdict: APPROVE / REQUEST_CHANGES / REJECT. Output JSON to `<this-dir>/<slot>-round-6.json`: `reviewer_id` ("<slot> <family>"), `verdict`, `confidence`, `rationale` (≤200 words), `concrete_changes_required` (empty if APPROVE), `findings`.
