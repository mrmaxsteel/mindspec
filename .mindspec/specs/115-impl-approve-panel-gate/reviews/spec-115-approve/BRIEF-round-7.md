# spec-115-approve — Round 7 Spec-Approval Panel (9 reviewers, three families)

**Worktree**: `/Users/Max/replit/mindspec/.worktrees/worktree-spec-115-impl-approve-panel-gate`
**Branch**: `spec/115-impl-approve-panel-gate` @ **50165d81a39ecc1ccbc09124dae104d8d723c611** (spec.md content = commit `e239c4e5`; `50165d81` adds the round-6 review artifacts). Base = `origin/main` (`f02a3a49`).
**Under review**: `.mindspec/specs/115-impl-approve-panel-gate/spec.md`.
**Panel**: 9 slots — F1–F3 Fable, O1–O3 Opus, **G1–G3 codex**. **Pass = ≥8 APPROVE, no REJECT.** Findings never out-voted (a confirmed finding is fixed/evidence-refuted, not out-voted — even at 8/9, as happened rounds 4 & 6).

**READ-ONLY**: verdict JSON only; pin reads to `50165d81`; ALL scratch under ABSOLUTE `/tmp`; NEVER edit the worktree; leave `git status` clean.

## What changed since round 6 — Option B (verify its IMPLEMENTATION; the disposition is SETTLED)
Round 6 (8/9) surfaced a genuine **TOCTOU transient-race** (G2 codex): the C+B `BranchExists` probe fails OPEN on a transient ref-lock/infra fault recovering within one `ApproveImpl` pass — `MergeBase@:249` succeeds → the scan's `BranchExists(bead/<id>)` transiently returns false (orphan masked, gate passes) → the transient clears → `FinalizeEpic` still merges the worktree-enumerated branch un-gated. Root cause: detection used `BranchExists` (`git rev-parse --verify refs/heads/bead/<id>`, the lockable ref) while the merge uses `WorktreeOps.List()` (`bd worktree list`) — two sources that desync. A 5-slot TOCTOU-disposition panel chose **Option B** (4/5 over A; see `reviews/spec-115-toctou-decision/DECISION-OUTCOME.md`). **The disposition is SETTLED — do NOT re-argue A vs B. Verify Option B is correctly + coherently implemented and the disclosed residual is honestly bounded.**

**Option B, as implemented in round 7 (commit `e239c4e5`, spec.md only):**
1. R1 gains a **worktree-enumeration merge-prevention leg** IN ADDITION TO (not replacing) the existing `BranchExists`/shared-predicate detection: the gate ALSO refuses if any `bead/<id>` worktree enumerated by the SAME source FinalizeEpic merges from (`bead.WorktreeList()` → `bd worktree list --json`, `internal/bead/bdcli.go:202`) belongs to a closed epic bead that is NOT an ancestor of the spec branch. So a transient ref-probe miss can no longer hide a candidate the merge loop will see. Fail-CLOSED on WorktreeList/ancestry error; epic-scoped. The shared `FindOrphanedClosedBeads` predicate stays byte-identical for complete/next/doctor + R3 (breadth: branchy-but-worktreeless orphans). ADR-0030-clean: `internal/approve` already imports `internal/bead` (`impl.go:13`); a bd query, not executor git-IO.
2. New **AC13** `TestApproveImpl_WorktreeEnumRefusesDespiteProbeMiss` (internal/approve): probe seam-forced to miss + worktree enumerated + non-ancestor → refuse (terminal fns uncalled), RED on revert-to-probe-only; + worktree-list-error → refuse; + no-false-positive subcases.
3. **HONEST residual** (Non-Goals): the round-6 probe-transient race is now CLOSED BY DESIGN by the leg (explicitly NOT claimed "physically implausible", NOT carried as an audited-refutation residual). Fact 2's conclusion honestly re-scoped to "PERSISTENTLY missing/corrupt ref" (the "no probe misclassification can produce an un-gated merge" overclaim deleted). The irreducible narrower residual IS disclosed: the gate's `List()` and FinalizeEpic's `List()` are two temporally-separated calls; a worktree CREATED between them is concurrent-lifecycle-mutation, out of scope under mindspec's serialized one-mutator-at-a-time model. Goal now discloses THREE residuals. Single-snapshot handoff noted as possible future hardening (not filed; OQ4 no-executor-seam stands).
4. **AC10 fix** (round-6 G3): the pre-applied `grep -l` proof replaced by a new named RED-on-revert test `TestLifecycleOwnershipExactlyOneWorkflowClaimant` (internal/lifecycle) parsing all domain OWNERSHIP.yaml, asserting `internal/lifecycle/**` claimed by exactly one domain = workflow.

## What to verify at `50165d81`
1. **Option B is cleanly + coherently implemented** — the worktree-enum leg is added to R1 correctly (added-to, not replacing; fail-closed on list/ancestry error; epic-scoped; the shared predicate byte-unchanged for complete/next/doctor). The grounded mechanism (`bead.WorktreeList()`, `bdcli.go:202`; approve already imports bead) is correct + ADR-0030-clean. R1/OQ2 describe the leg + posture precisely; no contradiction.
2. **The leg genuinely closes the round-6 TOCTOU** — because the gate now enumerates from the SAME source FinalizeEpic merges from, a transient `BranchExists` miss can no longer desync scan from merge. Verify this holds. (G2 lens: does the leg actually close YOUR round-6 finding?)
3. **The disclosed residual is HONESTLY bounded — no overclaim, no under-disclosure** — the Non-Goals must NOT claim total closure; the narrower two-`List()` concurrent-mutation residual must be honestly disclosed + correctly scoped-out via mindspec's lifecycle-serialization model. Fact 2's re-scope to "persistent" corruption must be accurate. Is there any OTHER un-gated-merge path (ordinary flow, no raw tamper) the leg + residual miss?
4. **AC13 & AC10 are sound + falsifiable** — both new named tests genuinely RED at `50165d81` (0 code hits — verify); AC13 pins the leg (probe-miss + live worktree → refuse; RED on revert-to-probe-only); AC10 pins the exactly-one-workflow invariant as RED-on-revert (not the pre-applied grep).
5. **No regression** — REFUSE core, R2/R3, recovery, epic-scope, ownership→workflow, Facts 1&2/AC11/AC12, ADR-0030/0037, never-false-refuse all intact. All 4 OQs `[x]`. Grounding exact.

## Per-slot lens
- **F1** falsifiability (AC13/AC10 + all discriminators RED); **F2** grounding (`bead.WorktreeList()` bdcli.go:202, approve imports bead, FinalizeEpic :383/:394/:402); **F3** contradiction/scope/OQ (the added leg vs the probe leg vs the three cleanly-signalled legs; the honest residual vs Fact 2 re-scope; all OQs).
- **O1** anti-gaming (does the leg close the round-6 TOCTOU with no new hole; is the residual honestly complete for the ordinary flow); **O2** ADR/ownership/blast-radius (the leg is bd-query ADR-0030-clean; shared predicate byte-unchanged; no fork of complete/next/doctor detection); **O3** decomposition + impacted-domains parity (internal/bead consumed read-only; still ~2-3 beads; AC13 in approve, AC10 in lifecycle).
- **G1** grounding empirical (verify `bead.WorktreeList()` + the FinalizeEpic loop cites + AC13/AC10 tests absent at 50165d81); **G2** (the TOCTOU finder) — verify Option B genuinely closes your round-6 transient-race, and judge whether the disclosed concurrent-mutation residual is honestly bounded (not under-disclosed); spec-correctness reasoning, NOT exploit-building; **G3** AC-runnability (AC13/AC10 + every AC1-AC13 proof RED at 50165d81, exact-named runs, no dangling refs to removed tests).

Verdict: APPROVE / REQUEST_CHANGES / REJECT. Output JSON to `<this-dir>/<slot>-round-7.json`: `reviewer_id` ("<slot> <family>"), `verdict`, `confidence`, `rationale` (≤200 words), `concrete_changes_required` (empty if APPROVE), `findings`.
