# spec-113-bead3 (R3, final bead) — Round 1: 7 APPROVE / 1 REQUEST_CHANGES (F1)

CODE CORRECT — decision-inertness confirmed by O2 (static: PanelGateDecision 10-leg ladder + ApproveThreshold never read Panel.Gate; only advisory readers) + R8 (empirical AC3: stamp/reject/byte-identical/advisory-read-through all correct). Byte-identity + enum-validation + gate-scoped resolvers all mutation-PINNED (F1). One test-pinning finding, adjudicated per standing rule:

## Finding to fix (F1-1)
Decision-inertness is under-pinned for the values THIS bead newly exposes. 112's TestPanel_GateFieldDecisionInert only loops gates {"", "bead", "weird"} — a mutation keyed to `Gate == "final_review"` (or any of spec_approve/plan_approve/adhoc) SURVIVES the entire 4-package suite, and R3 makes exactly those 5 enum values CLI-writable via --gate. FIX (F1-proven, additive): add ONE NEW test that loops ALL FIVE config.PanelGateKeys through PanelGateDecision + ApproveThreshold on an otherwise-identical fact set, asserting the Allow/Block decision AND the threshold are IDENTICAL regardless of the gate value (i.e. gate is decision-inert for every enum value, not just 3). Leave 112's TestPanel_GateFieldDecisionInert UNMODIFIED. Proof-of-done: the surviving mutant (make ApproveThreshold behave differently when Gate=="final_review") must now RED the new test. Keep internal/panel's non-test config-free-leaf invariant intact (a *_test.go config import is a test-only dep and doesn't break `go list -deps ./internal/panel`, but if simpler, hardcode the 5 values with a comment citing config.PanelGateKeys as the source of truth).

Other F1 probes: byte-identity PINNED (omitempty drop reds tests), enum-validation no-bypass (defense-in-depth: gateKeyError rejects before write even with isValidPanelGateKey+rejectControlBytes gutted), gate-scoped resolvers PINNED. O1 informational (predicted config-package edits unneeded — correct). No other changes.
