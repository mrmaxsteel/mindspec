# spec-113-bead1 (R1, LOAD-BEARING) — Round 1: 7 APPROVE / 1 REQUEST_CHANGES (F1)

CODE IS CORRECT — R8 + F1 + S2 empirically confirmed the fix end-to-end (non-bead panel BLOCKs after target advance, tally exits 1, missing-ref honest Warn naming the target, bead panels byte-identical, zero internal diff, tallyExitAction 2-arg). All findings are TEST-strengthening; per the standing rule every one is adjudicated (fixed), not out-voted.

## Findings to fix (amend the single bead commit)
1. **[F1 M5 — load-bearing, mutation survived]** `TestPanelVerify_NonBeadMissingTargetRef` wraps the real gitutil.ErrRefNotFound but its assertions are ALSO satisfied by the leg-(5b) GitErr rendering, so the IsRefNotFound classification is executed-but-not-pinned: replacing `IsRefNotFound: exec.IsRefNotFound` with `func(error)bool{return false}` leaves the whole cmd/mindspec suite GREEN. FIX (F1-proven): add a distinctive assertion for the missing-ref phrase — `strings.Contains(out, "no longer exists")` — that reds under the mutation and passes on pristine code. Uniquely pins the missing-ref (leg 5) vs transient (leg 5b) distinction.
2. **[O2 + F1 + R8]** `TestSanitizeNonBeadDecision` table omits leg (4) round-mismatch (all rows Round==1) and leg (3) abandoned. Both are provably leak-free but uncovered. FIX: add a `RoundMismatch:true` row and an `Abandoned:true` row (built from the real PanelGateDecision like the existing rows), asserting no `bead/` interpolation + Action preserved.
3. **[S2]** Leg 8 (incomplete: fewer seeded verdicts than ExpectedReviewers) is proven at the sanitize unit level but lacks a full E2E CLI (runPanelVerbCmd) test on a non-bead panel — and the spec's R1 falsification explicitly names the incomplete case. FIX: add an E2E test that seeds a non-bead panel with fewer verdict files than expected_reviewers at an un-advanced target and asserts verify shows no PASS / tally exits non-zero / no bead/ leak.
4. **[R8 cosmetic]** Two double-space / empty-value message artifacts reachable ONLY via a hand-corrupted panel.json (not any CLI path). FIX if a trivial message-template cleanup; otherwise the implementer documents why they're CLI-unreachable (evidenced refutation).

Zero-diff fence, tallyExitAction 2-arg, and pinned-test-unmodified all re-confirmed by O1/O3/S1/S3. The fix round must keep all of those true (test-file additions only; still zero internal diff).
