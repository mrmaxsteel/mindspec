# spec-113-plan-approve — Plan-Approval Panel (9 reviewers, three families)

**Worktree**: `/Users/Max/replit/mindspec/.worktrees/worktree-spec-113-panel-verb-workflow-followups`
**Branch**: `spec/113-panel-verb-workflow-followups` @ **001ed3a5dcc74ab6431b3a48c1d2befcb5461f92**
**Under review**: `.mindspec/specs/113-panel-verb-workflow-followups/plan.md` (the DRAFT plan, before `mindspec plan approve`). Read `spec.md` (same dir) for the requirements the plan implements.
**Panel**: 9 slots — F1–F3 Fable, O1–O3 Opus, **G1–G3 codex (real codex)**. **Pass = ≥8 APPROVE, no REJECT.**

**READ-ONLY RULE (MANDATORY)**: verdict JSON only; pin reads to `001ed3a5`; scratch under ABSOLUTE /tmp only; leave `git status` clean. (Relative `.mindspec/` writes + harness cwd-reset corrupt sibling worktrees — never do it.)

## What this plan is
The implementation plan for spec 113 — a followup-hardening wave of **4 bounded fixes**, decomposed **1:1 into 4 beads** (pre-existing IDs, children of epic `mindspec-r6hk`):
- **Bead 1 = mindspec-4d9m (R1, P2, LOAD-BEARING, lands FIRST)** — non-bead panel staleness. Today `resolvePanelGateFacts` (cmd/mindspec/panel.go ~302-306) guards on `IsBead()`, leaving `beadID=""` for non-bead panels → `internal/panel/gate.go` (~372) rev-parses literal `bead/` → MissingRef → leg (5) Warn with the malformed `references branch bead/` message (gate.go:186-189), short-circuiting the staleness/vote legs. The plan's fix: gather non-bead target facts **entirely in `cmd/mindspec`** (a non-bead `GateIO.RevParse` closure over `panel.json.target` via the `revParseForPanelFn` seam) so `internal/panel`/`internal/instruct`/`internal/complete` get a **ZERO-BYTE diff**; message hygiene is a CLI-layer `sanitizeNonBeadDecision` that strips the `panel.RawMergeFence("")` constant + replaces empty-interpolation fragments with target-naming text, never touching `Action`.
- **Bead 2 = mindspec-lczt (R2, P3)** — extend `SHELL_METACHAR_RE` (ms-panel.js:138) to reject bare `$`; regenerate `.claude/workflows/ms-panel.js` byte-identical; floor tests green.
- **Bead 3 = mindspec-zw81 (R3, P3)** — `panel create --gate <name>` validated vs `config.PanelGateKeys` (5-key enum), stamps decision-inert `Panel.Gate`, resolves defaults via `PanelGateExpectedReviewers`/`PanelGateApproveThresholdExpr`; invalid gate → non-zero + ADR-0035 5-key recovery line; omit `gate` key when flag absent (112-R9 byte-identity).
- **Bead 4 = mindspec-cthj (R4, P3)** — reconcile 112 R4-vs-R1 → resolve-to-family. Code ALREADY resolves-to-family (`Reviewer.model()` config.go:139-144); this is a CODE COMMENT superseding 112 R4's phrase + a pinning test `TestLoad_EmptyStringModel` (accept `{model:"",family:codex}`→codex; refuse `{model:""}` alone). NO struct change.

## What to verify (this is a PLAN panel — assess the plan, and GROUND every claim in the real code at 001ed3a5)
1. **Decomposition (F1/O3/G3)**: 4 beads, target count 3-5 ✓. Confirm ZERO false `depends_on` edges (longest chain =1); the Bead1→Bead3 ordering is merge-scheduling only (both edit cmd/mindspec/panel.go + internal/panel — R1 first as P2). No bead is secretly two; none should merge (R_scope check). Each bead one-round bead-panel verifiable.
2. **R1 design soundness (O1/G2 — LOAD-BEARING)**: does the CLI-layer approach actually work? Read `cmd/mindspec/panel.go` (`resolvePanelGateFacts`, `revParseForPanelFn`, verify/tally verbs), `internal/panel/gate.go` (`ResolveGateFacts`, `PanelGateDecision`, `GateIO`, `RawMergeFence`), `internal/instruct/panelstate.go` (`verdict()` ~99-136 — it consumes `d.Message`). Confirm: (a) feeding the non-bead target SHA vs `reviewed_head_sha` into the existing legs un-shadows legs (6)/(8)-(10); (b) the claimed **zero-byte diff** to internal/panel+instruct+complete is actually achievable (the plan asserts `git show --name-only` lists none of the three — is that real given where the message is rendered?); (c) `sanitizeNonBeadDecision` in the CLI layer can strip `RawMergeFence("")` + fix the message WITHOUT mutating the shared `Decision.Message` (which instruct/complete read). If the message MUST change in `internal/panel` to fix the bug, the zero-diff claim is false → REQUEST_CHANGES.
3. **Testing Strategy (O2/G1)**: each AC has a real, runnable test. Scrutinize AC1's routing of the non-bead staleness stub through `revParseForPanelFn` — the plan notes this seam is today only `panel create`'s; confirm the plan extends it soundly (doc comment + missing-ref rows stubbing errors wrapping `gitutil.ErrRefNotFound` so `IsRefNotFound` is exercised). Confirm `TestLoad_EmptyStringModel` (R4), the node metachar check + `cmp` mirror + `go test ./plugins/mindspec` (R2), and the `panel.json` gate-key + `config show --gate` cross-check (R3) are all specified + runnable.
4. **ADR Fitness (O3/O2)**: verdicts correct? ADR-0037 (single decision home preserved — zero gate.go diff), ADR-0035 (recovery lines), ADR-0040 (L2 CLI / L3 workflow layering), ADR-0030 (evaluated, deliberately NOT cited — its domains don't intersect workflow/core; a citation would trip `adr-cite-irrelevant` — verify that reasoning). Confirm carry-forward #2 is honored: the gate-vocabulary provenance re-attributed to **spec 112**, NOT ADR-0034.
5. **Provenance (O3)**: every spec AC (AC1→R1, AC2→R2, AC3→R3, AC4→R4, AC-global) mapped to bead verification steps.
6. **Grounding (F2/G1)**: verify the plan's cited line numbers/seams/symbols against the real tree — `revParseForPanelFn`, `RawMergeFence`, `PanelGateKeys`, `Reviewer.model()`, `PanelGateAdvisoryDefault`, the byte-identity pin tests. A claim the tree doesn't support is a finding.
7. **Carry-forwards folded (F3/all)**: the 4 spec-panel carry-forwards must be present — (1) O1 CLI-layer message mandate in Bead 1; (2) O2 ADR-0034 re-attribution in ADR Fitness; (3) F2 legs-(2)/(4)-can-Block precision; (4) F3 verify=read-only / tally=block-capable wording.

## Per-slot lens defaults
- **F1 Fable** — decomposition soundness (bead count, false-dep edges, sizing, ordering); **F2 Fable** — grounding (plan claims tied to real code + line numbers); **F3 Fable** — per-bead falsifiability + contradiction (plan vs spec vs itself) + carry-forwards present.
- **O1 Opus** — R1 design soundness + the zero-byte-internal-diff claim (LOAD-BEARING); **O2 Opus** — Testing Strategy completeness (each AC → real runnable test; the revParseForPanelFn seam extension); **O3 Opus** — ADR Fitness + Provenance mapping + ADR-0034 re-attribution.
- **G1 codex** — empirical grounding (read the seams/tests; verify the plan's factual claims live); **G2 codex** — R1/R3 design feasibility empirically (can sanitizeNonBeadDecision keep zero internal diff? does --gate omitempty preserve byte-identity?); **G3 codex** — completeness/integration (4 beads land cleanly, tests runnable, no missing step, R1→R3 merge-adjacency handled).

Verdict: APPROVE / REQUEST_CHANGES / REJECT. Output JSON to `<this-dir>/<your-slot>-round-1.json` with keys: `reviewer_id` ("<slot> <family>"), `verdict`, `confidence` (0–1), `rationale` (≤200 words), `concrete_changes_required` (empty if APPROVE), `findings`.
