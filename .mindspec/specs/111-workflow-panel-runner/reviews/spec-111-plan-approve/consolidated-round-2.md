# spec-111-plan-approve — consolidated round-2 changes (for the round-3 fix)

Round-2 tally: 5 APPROVE (F2 0.92, O1 0.85, O2, O3 0.90, G3) / 4 REQUEST_CHANGES (F1 0.80, F3 0.75, G1 0.88, G2 0.78) / 0 REJECT → round 3. All 10 round-1 items were judged ADDRESSED or PARTIAL; the residuals are FIVE surgical plan-text items, heavily convergent (four reviewers hit item 1 independently).

1. **(G1+G2+F3 convergent — must) Fix the malformed-once shim scenario's impossible comparison.** First payload is "narrative prose (no JSON)" but the value-fidelity clause (carry-forward #2) requires canonically decoding the FIRST attempt to compare gate fields — no operand exists. Fix: first payload = ONE schema-valid verdict JSON object WRAPPED in disallowed narrative (rejected by the exactly-one-object/ambiguity rule, canonically comparable on re-serialize); specify the exact first and second payload shapes verbatim. (Optionally add a separate pure-narrative no-JSON scenario whose check is MISSING-path-only, no fidelity clause.)
2. **(F1 — must) Reconcile buildCommand call-site literals with the enumeration test.** Steps prescribe `buildCommand("mindspec panel create", ...)` string literals while Step 6's test forbids ANY mindspec-bearing literal outside the ALLOWED_CLI array — the plan's prescribed code fails its prescribed test (same class as round 1's snippet contradiction). Fix per F1: destructure named verb constants FROM the ALLOWED_CLI array (call sites use identifiers, never literals) + pin the identifier count in the static test + add a leading-dash argument-injection guard clause to buildCommand.
3. **(G2 — must) Extend the input contract to `target`.** Traversal/control-byte validation explicitly covers `target` (branch-name-safe grammar + argv-safe quoting), and the e2e gains target metacharacter/argument-injection + control-byte cases (scenario 7 currently probes only one unsafe slug).
4. **(F3 — must, one line) Seed the e2e scratch config's `panel:` mix.** `panel create` stamps expected_reviewers from the config resolvers; the e2e never sets a mix, so every run reads incomplete and runs 1/3's completeness observables can't discriminate. Add the explicit `panel:`/`gates:` block to the e2e's scratch config setup.
5. **(F1 note, fold into item 2's test text) Close the exact-match bypass reading**: the enumeration/chokepoint assertion must not be satisfiable by an exact-literal + raw-arg concatenation that bypasses buildCommand — the identifier-count pin (item 2) is the mechanism; state it as such.

Constraints: plan-text only; ONE commit `docs(spec-111): apply round-2 plan-panel changes`; beads ≤7 steps; carry-forwards + round-1 items must not regress; validate plan passes. **Round 3 timing: WAIT for the codex reset (12:37 AM) — G1/G2 must re-verify their own asks; do not sonnet-sub dissenting voters.**
