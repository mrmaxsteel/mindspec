# spec-112-plan-approve — Plan-Approval Panel (Round 1, 9 reviewers, three families)

**Spec worktree**: /Users/Max/replit/mindspec/.worktrees/worktree-spec-112-per-gate-panel-config
**Under review**: `.mindspec/specs/112-per-gate-panel-config/plan.md` @ **e06c0b76393fc72f492867b4a1d19fc80ceb2d31** (also read the approved `spec.md` beside it — the contract the plan must deliver). The branch is rebased onto post-109 main, so 109's landed surface is present in the worktree.
**Panel**: 9 reviewers, three families — F1–F3 Fable, O1–O3 Opus, G1–G3 GPT-5.5 (codex). Pass = **≥8 APPROVE, no REJECT**; any REJECT halts; hard_block blocks regardless.
**This is a PLAN-approval gate**: review the PLAN's decomposition, coverage, implementability, and DAG soundness against the approved spec — NOT the spec itself (it passed its own 9/9 panel) and NOT code (none written yet).

## READ-ONLY RULE (mandatory, all 9)

Repo files READ-ONLY; write only your verdict JSON; scratch under /tmp; pin reads to the SHA.

## What the plan proposes

Spec 112 extends 109's `panel:` config to per-gate reviewer mixes (per-gate `gates:` map, generalized `{model, lens, count}` reviewer entry with open model vocabulary + an interleaved global-cursor default-lens rule, `substitutes:` map, decision-inert recorded `gate` field on panel.json, gate-aware ReviewerCountNote, `config show --gate [--json]`, and R9's stable downstream contract). The plan decomposes this into **3 beads**:

- **Bead 1 — internal/config (R1–R5).** gates map + generalized reviewers + substitutes + per-gate refusals + the three gate-scoped resolvers with deterministic interleaved-cursor slot expansion + known-model advisory list. Key design calls: `Reviewer.Count` becomes `*int` (absent → 1 = R2's monotone relaxation; explicit `count: 0` refused); `count()`/`model()` accessors single-home family/model resolution; a single ordered `panelGateKeys` slice feeds validation + recovery lines + Bead 3's enum-order render; R4's threshold-bound + cross-field-inheritance refusal is one resolved check over every gate's chain-resolved (expr, sum); adds `PanelGateAdvisoryDefault(recordedGate, isBead)` as the single home of R7's caller-side selection. Files: config.go, config_test.go, core/interfaces.md. Deps: none.
- **Bead 2 — internal/panel (R6).** recorded decision-inert `gate` field (like `abandon_reason`) + the ADR-0037 second amendment note (doc-only, §§3/6/8 untouched). Leaf invariant + `threshold > 0` guard re-verified. Files: panel.go, panel_test.go, ADR-0037, workflow/architecture.md. Deps: none.
- **Bead 3 — internal/complete + cmd/mindspec (R7–R9).** gate-aware advisory (calls Bead 1's `PanelGateAdvisoryDefault`), config show gates/substitutes/known-model + escaping, `--gate [--json]` (typed struct via `encoding/json`, R9 members, `substitution.in_force`), R9 stable-contract doc. Files: cmd/mindspec/config.go+test, complete.go, panel_advisory.go+test, workflow/interfaces.md+architecture.md. Deps: Beads 1 AND 2 (real compile-time edges).

**DAG: Bead 1 ‖ Bead 2 → Bead 3** (longest chain 2, 3 beads). Provenance maps R1–R9 + AC1–AC~12 + Validation Proofs (17 rows). ADR Fitness: all six evaluated, no divergence. Plan-time regression: only the known pre-existing `internal/instruct` `TestRun_IdleNoBeads` (`z4ps`) fails — no bead touches that package. (Note: the plan corrects 109's mis-attribution of that flake — it's internal/instruct, not internal/next.)

## Slot lenses (each writes `<slot>-round-1.json`; reviewer_id "<slot> <family>")

- **F1 (Fable) — provenance/coverage completeness**: does EVERY spec requirement R1–R9 AND every spec AC (AC1..) + Validation Proof trace to a concrete bead + a runnable verification step? Any requirement or AC with no delivering bead, or a Provenance row that points at a verification which wouldn't actually prove it?
- **F2 (Fable) — decomposition soundness**: apply the decomposition heuristics — is the 3-bead split right (independence test; merge signal — do ≥3 beads touch the same file?; trivial-work test; ≤5 beads; serial chain ≤3)? Is folding the ADR-0037 amendment into Bead 2 correct? Is Bead 1 (R1–R5) too big / should it split, or correctly one bead because the R4 check runs the R3 chain?
- **F3 (Fable) — adversarial coverage-refuter**: pick ≥3 spec requirements/ACs and try to find one that NO bead actually delivers, or a bead Verification whose command wouldn't actually falsify a broken implementation (e.g. the cursor-start-0 pin, the escaping AC, the adhoc inheritance-range refusal). Report per-claim delivered/gap.
- **O1 (Opus) — implementability vs the real 109 code**: are the plan's design calls actually implementable against the landed tree? Read `internal/config/config.go`, `internal/panel/panel.go`, `internal/complete/panel_advisory.go`, `cmd/mindspec/config.go`. Is the `Reviewer.Count *int` change safe against 109's existing count usages? Do `PanelGateAdvisoryDefault`, the `panelGateKeys` slice, the R4 chain-resolved check, and Bead 3's `encoding/json` struct fit the existing code cleanly? Any step that can't be built as written?
- **O2 (Opus) — contract & ADR fitness**: is the `## ADR Fitness` section correct (0040/0037/0035/0034/0023/0039)? Is single-interpreter genuinely preserved (config-side resolvers RAW; resolution single-homed in panel.ApproveThreshold)? Leaf invariant (internal/panel no internal/config import)? Any ADR divergence the plan smuggles without flagging? Is the ADR-0037 amendment scoped (§§3/6/8 untouched)?
- **O3 (Opus) — bead boundaries / file overlap / DAG**: do the 3 beads have clean file boundaries (merge-signal check: is any file touched by 2+ beads)? Is the B1‖B2→B3 DAG real (Bead 3's deps on 1 AND 2 are genuine compile-time edges; Bead 1/2 truly independent)? Any false dependency or hidden shared-state coupling? Is the parallelism claim sound?
- **G1 (codex) — test runnability & AC-name fidelity**: are the per-bead Verification commands real and runnable, and do the test names match the spec's AC test names (TestPanelGateSlots_DeterministicExpansion, TestConfigShow_HostileStringsEscaped, TestLoad_RefusesPerGateKnobs, etc.)? Is the protocol-YAML proof (sums 9/9/6/12, final_review 12 slots/"11", adhoc≡bead) sound and reproducible? Build the tree if useful to check the named symbols exist as 109-landed.
- **G2 (codex) — security carry-through**: does Bead 3's steps actually implement the spec's now-REQUIRED escaping (escapeConfigValue-equivalent for note/model/lens/substitutes in config show + --gate text) AND the real-JSON-encoder pin for `--gate --json`? Is the `substitution.in_force` member + hostile-bytes verification present in Bead 3's plan? Any escaping gap the plan leaves for a bead to forget?
- **G3 (codex) — downstream contract delivery**: does Bead 3 actually deliver R9's stable `--gate --json` schema + the recorded `gate` field that 110's writer / 111's runner consume, with the map-order determinism (gates→enum, substitutes→sorted)? Is `substitution.in_force` ("substitutes"|"claude_sub_on_quota") a sound resolution of the spec's open sub-member name? Does the plan keep 112 from stealing 110/111's scope?

Verdict: APPROVE / REQUEST_CHANGES / REJECT. Output JSON to `.../spec-112-plan-approve/<slot>-round-1.json` with keys: reviewer_id, verdict, confidence, rationale (≤200 words), concrete_changes_required (empty if APPROVE), findings (hard_block only for a genuine blocker).
