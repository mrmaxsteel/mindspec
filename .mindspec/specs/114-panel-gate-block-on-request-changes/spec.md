---
approved_at: "2026-07-10T06:44:02Z"
approved_by: user
status: Approved
---
# Spec 114-panel-gate-block-on-request-changes: Panel gate blocks on any unresolved REQUEST_CHANGES, with audited-refutation escape

## Goal

Close the last silent out-vote in the authoritative `mindspec complete` panel gate: today a reviewer's REQUEST_CHANGES finding can be out-voted by the approve count (the default N−1 threshold tolerates one dissent), so a real, evidenced finding is droppable without anyone recording why. Two operator decisions, already made, are encoded here:

1. **R1 (gate change)** — any **unresolved REQUEST_CHANGES blocks `mindspec complete`, exactly like a REJECT**. The gate passes only when every expected reviewer's latest-round verdict is APPROVE, or is a REQUEST_CHANGES that has been explicitly refuted (Decision 2). This is a genuine change to the `PanelGateDecision` matrix and amends **ADR-0037**.
2. **R2 (spurious-finding escape)** — because a mechanical gate cannot judge whether an RC is correct, the operator/orchestrator can clear ONE specific reviewer slot's REQUEST_CHANGES by recording an **audited refutation** (slot id + reason + evidence), following the existing abandon precedent: always audited on bead metadata, never silent. A refuted RC no longer blocks; the audit entry persists. This is the only agent-settable escape for a specific RC — the human-only `MINDSPEC_SKIP_PANEL` hatch and the whole-panel abandonment procedure are unchanged.

Target user outcome: an operator or orchestrating agent running `mindspec complete` **cannot silently out-vote a reviewer's REQUEST_CHANGES by hitting the count**; a genuinely-spurious RC is cleared only via an explicit, audited, per-slot refutation that leaves a permanent record on the bead — the same "legitimate precisely because it is always audited, never silent" property ADR-0037 §7 already grants abandonment.

## Background

**Where the out-vote lives today.** The single decision home is `panel.PanelGateDecision` (`internal/panel/gate.go:131-263`). Its short-circuit legs (comment block at gate.go:110-127) end with:

- leg (9), gate.go:245-253 — `f.Res.Rejects > 0 || len(f.Res.HardBlocks) > 0` → Block ("no vote count overrides an evidence-bearing gate");
- leg (10), gate.go:255-262 — `f.Res.Approves >= threshold && threshold > 0` → **Allow**, where `threshold` is `Panel.ApproveThreshold()` (`internal/panel/panel.go:113-127`): default **N−1**, optionally overridden per-panel by the recorded `approve_threshold` expression (spec 109 / ADR-0040).

A REQUEST_CHANGES verdict is counted as *neither* an APPROVE nor a REJECT: the tally switch at `internal/panel/tally.go:296-301` increments `Approves` only for `VerdictApprove` and `Rejects` only for `VerdictReject`; per the `Result` doc (tally.go:107-111), "REQUEST_CHANGES (and anything unrecognized) is neither." So on the default 6-reviewer panel (threshold 5): **5 APPROVE + 1 REQUEST_CHANGES, complete verdicts, fresh SHA → `Approves` = 5 ≥ 5 → Allow at gate.go:257-258.** The RC's `concrete_changes_required` never has to be addressed or even acknowledged; the merge proceeds and no record of the dissent survives on the bead. (The same arithmetic lets one *unrecognized* verdict string ride through — it is also "neither".) The vote-only advisory twin `Result.VoteDecision` (`internal/panel/tally.go:180-214`, threshold legs at 206-213) reproduces the identical pass, by design ("can never disagree on the vote portion").

**Every surface shares this decision, so fixing it once fixes them all.** The authoritative in-binary gate is `panelGate` (`internal/complete/panel_advisory.go:140-215`), invoked at complete.Run step 2.25 (`internal/complete/complete.go:337-378`) before any mutation; on Block it returns a `guard.NewFailure` whose final line is an ADR-0035 recovery command (panel_advisory.go:194-207). The read-only surfaces — `mindspec panel verify`/`panel tally` (`cmd/mindspec/panel.go:7-10`, pinned by `TestPanelVerbs_DecisionIsPanelGateDecision`) and `mindspec instruct --panel-state` (`internal/instruct/panelstate.go:121`) — delegate to the same `PanelGateDecision`, and complete's warning-only `panelAdvisory` (panel_advisory.go:48-77) renders `VoteDecision`.

**The audit precedent to follow (Decision 2).** Abandonment is the existing "legitimate exit" shape: `"abandoned": true` + mandatory `abandon_reason` recorded on `panel.json` (`internal/panel/panel.go:58-68`, deliberately parse-lenient), honored by the gate as Warn/Allow at leg (3) (gate.go:160-170), and **audited at complete time**: `writePanelAuditMetadata` (`internal/complete/panel_advisory.go:304-342`) writes `panel_abandoned` + `panel_abandoned_at` + `panel_abandoned_reason` (and, for the env hatch, `panel_gate_skipped` + `panel_gate_skipped_at`) to bead metadata via `completeMergeMetadataFn` → `bead.MergeMetadata` (`internal/bead/bdcli.go:263-266`), called at step 5.6 (`internal/complete/complete.go:778-785`) only after the terminal mutation succeeds, best-effort. The operator procedure is documented in `plugins/mindspec/skills/ms-panel-tally/SKILL.md:84`. The refutation record follows this exact pattern: a recorded repo-file structure the gate reads, plus a persistent audit entry on the bead.

**Trust boundary is unchanged.** Per ADR-0037 §8, every gate input is an agent-writable repo artifact; the refutation record is exactly as agent-writable as `abandoned` is today. This spec adds an anti-footgun (a finding can no longer vanish *by arithmetic*, without a trace); it does not attempt anti-adversary tamper-resistance, which §8 explicitly forbids "fixing."

## Impacted Domains

- **workflow**: `internal/panel/**` (the decision matrix in `gate.go`, the tally/`VoteDecision` in `tally.go`, the `panel.json` schema in `panel.go`), `internal/complete/**` (the authoritative gate wiring + the new refutation audit write in `panel_advisory.go`), `cmd/**` (the panel verbs inherit the new decision; any refutation-recording surface), and `plugins/mindspec/**` (the /ms-panel-tally refutation procedure documentation) — all owned by workflow per `.mindspec/domains/workflow/OWNERSHIP.yaml` (lines 2, 14, 19-20). No other domain is touched: the audit write reuses the existing `bead.MergeMetadata` seam read-only (execution domain owns `internal/bead/**` but no change lands there).

## ADR Touchpoints

- [ADR-0037](../../adr/ADR-0037-panel-gate-enforced-contract.md): **amended by this spec** — the panel-gate enforced contract. **§1** (registration schema) gains the optional `refutations` array on `panel.json` (entries `{slot, round, reason, evidence}`, parse-lenient), the same schema-addition precedent §1 already records for 112's `gate` field and 109's `approve_threshold`. **§3** ("Threshold: `expected_reviewers − 1`, defined once" — "one dissent tolerated") gains the new rule: the threshold remains a necessary floor on genuine APPROVE count, but is no longer sufficient — every expected reviewer's latest-round verdict must be APPROVE or an explicitly-refuted REQUEST_CHANGES; an unresolved RC blocks exactly like a REJECT, and a refuted RC never counts toward the floor (so refutation cannot buy past a sub-threshold panel — ADR-0040's recorded `approve_threshold` stays meaningful). **§7** (escape hatches) gains the **audited refutation** alongside skip/abandonment/config-toggle, with the "legitimate precisely because it is always audited, never silent" contract — and, because a refutation CHANGES the gate outcome (unlike the terminal-exit abandonment/skip audits, which are best-effort), its `panel_refuted` audit is **durable**: a metadata-write failure on an applied refutation fails the completion rather than silently clearing the gate. Like the `MINDSPEC_SKIP_PANEL` hatch, the block message must NOT advertise a paste-able refutation incantation; the escape is documented (§7 + /ms-panel-tally) but not reflex-suggested at the block. **§6** (fail-open without a panel, fail-closed with one) and **§8** (trust boundary: anti-footgun, not anti-adversary — the record is agent-writable by design; an empty `reason` still clears, a documented footgun in parity with empty `abandon_reason`) are unchanged and must be re-affirmed in the amendment text.
- [ADR-0035](../../adr/ADR-0035-agent-error-contract.md): agent error contract. The new RC Block message must name the unresolved slot(s) and end with a genuine `recovery:` line (the existing Block path already routes through `guard.NewFailure`, `internal/complete/panel_advisory.go:194-207`; the convention test `internal/guard/recovery_convention_test.go` enforces the final-line rule).
- [ADR-0040](../../adr/ADR-0040-orchestration-layering-ratchet.md): the recorded `approve_threshold` override (spec 109) layers under this change — a recorded threshold below N−1 still sets the approve *floor* but no longer licenses unrefuted dissent (see R1's chosen model and OQ1). The amendment must state this interaction explicitly so 109's extension and 114's rule cannot be read as contradicting.
- [ADR-0023](../../adr/ADR-0023.md): beads as single state authority. The refutation record on the review artifact is a *review artifact, not workflow state* (ADR-0037 §8 closing paragraph); the durable audit lives on bead metadata exactly like `panel_abandoned`.

## Requirements

1. **Any unresolved REQUEST_CHANGES blocks `mindspec complete`, exactly like a REJECT.** `panel.PanelGateDecision` (`internal/panel/gate.go:131-263`) gains a new blocking condition, **layered on top of** the existing matrix (chosen model — layer, not replace; OQ1 resolved): the `approve_threshold`/N−1 floor stays a **necessary** condition on the count of **genuine `APPROVE`** verdicts, and *in addition* the gate Blocks unless **every** latest-round verdict among the complete set is either (a) canonical `APPROVE`, or (b) canonical `REQUEST_CHANGES` covered by a recorded refutation for that slot at that round (R2). A refuted RC is removed from the *blocking* set but **never** increments `Approves` (tally.go must not count it as APPROVE), so refutation can never buy past the approval floor: an **at-threshold** panel unblocks on refutation (e.g. 7 APPROVE + 1 refuted RC, threshold 7 → passes), but a **sub-threshold** panel still Blocks on the floor even after every RC is refuted (e.g. 6 APPROVE + 2 refuted RC, threshold 7 → 6/7 still blocks; the recourse is a re-panel for genuine approvals, not refutation). This keeps the recorded `approve_threshold` (spec 109 / ADR-0040) meaningful. An unrefuted REQUEST_CHANGES — and equally an unrecognized verdict string, which is likewise "not an APPROVE" (tally.go:107-111) and is NOT refutable — Blocks. All other blocking conditions are byte-preserved: incomplete panels (leg 8), stale SHA (leg 6), round mismatch (leg 4), dirty tree (leg 7), malformed registration (leg 2), REJECT/hard_block (leg 9), and the `Approves >= ApproveThreshold()` floor (leg 10) all still block exactly as today. **Message format (OQ4 resolved — do NOT advertise the escape)**: the Block message names the offending unresolved slot(s) by slot id, never prints the skip variable (HC-7), carries the raw-merge fence in its body, and ends with an ADR-0035 `recovery:` line whose PRIMARY advice is fix-and-re-panel (`/ms-bead-fix` + `/ms-panel-run step 0`); it does **not** print a paste-able refutation command/incantation — exactly as the gate never prints a paste-able `MINDSPEC_SKIP_PANEL` prefix. The refutation escape is discoverable in ADR-0037 §7 and the /ms-panel-tally skill, not reflex-advertised at the block, so it stays a deliberate looked-up action. The vote-only twin `Result.VoteDecision` (`internal/panel/tally.go:180-214`) moves in lockstep so complete's advisory line, `panel verify`/`panel tally`, and `instruct --panel-state` — all of which delegate to the same decision (`cmd/mindspec/panel.go:7-10`, `internal/instruct/panelstate.go:121`) — can never disagree with the gate on the vote portion. **Falsified if**: a panel with complete verdicts and a fresh SHA carrying N−1 APPROVE + 1 unresolved REQUEST_CHANGES still yields Allow from `PanelGateDecision` or a zero exit from `mindspec complete`; if a **sub-threshold** panel whose RCs are ALL refuted (e.g. 6 APPROVE + 2 refuted RC, threshold 7) yields Allow instead of blocking on the floor; if any OTHER blocking condition (REJECT, hard_block, incomplete, stale SHA, round mismatch, dirty tree, malformed registration, sub-threshold) stops blocking; if a refuted RC increments `Approves`; if the Block message prints a paste-able refutation incantation; if an unrecognized verdict string can pass the gate or be cleared by a refutation; or if `VoteDecision` reports the vote portion would pass while `PanelGateDecision` blocks on it (or vice versa) for the same tally.
2. **Audited refutation escape — per-slot, always recorded, never silent.** An operator/orchestrator clears ONE specific reviewer slot's REQUEST_CHANGES by **hand-editing `panel.json`** (OQ3 resolved — a plain repo-file edit, abandon-parallel; the gate does the validation, not the recording surface), adding to a new **`refutations` array** (OQ2 resolved — array on `panel.json`, NOT per-slot files) one entry with the exact schema `{"slot": <string>, "round": <int>, "reason": <string>, "evidence": <string>}`, where `slot` is the filename-derived `Verdict.Slot` (`internal/panel/tally.go:43-59`) and `round` is the round it refutes. The array is parse-lenient like `abandon_reason` (`internal/panel/panel.go:58-68`): a missing/empty field is a consumer concern, never a parse error that would tip the gate fail-open. Effects, all mandatory and enforced **in the gate** (`internal/panel` + `internal/complete`), not at record time: (a) a refutation clears a slot's RC only when that slot's **latest-round** verdict is canonical `REQUEST_CHANGES` and the entry's `round` matches that latest round — the panel then unblocks iff no other blocking condition remains; (b) **DURABLE audit — not best-effort**: because a refutation CHANGES the gate outcome (it clears a block, unlike abandonment which is a terminal non-merge exit), `mindspec complete` MUST NOT complete-successfully unless the `panel_refuted` metadata for the ACTUALLY-APPLIED slot+round refutation(s) is durably written to bead metadata; a `MergeMetadata` failure on an applied refutation must FAIL the completion (fence: exit non-zero, do not silently merge), NOT be swallowed as the warning `writePanelAuditMetadata` uses today (`internal/complete/panel_advisory.go:316-342`, called only after `completeErr == nil`, warns-and-continues) for the abandon/skip audits. To record only genuine clears, the set of refutations that the gate ACTUALLY APPLIED (each matched a latest-round `REQUEST_CHANGES` — never a stale, invalid, REJECT-targeting, or unused entry) is plumbed OUT of the decision (`PanelGateDecision`/`panelGate`, `internal/panel/gate.go` + `internal/complete/panel_advisory.go:140-215`) into the audit write; the entry carries a `panel_refuted` key (parallel to `panel_abandoned`) plus the applied slot(s)/round(s)/reason(s) and a timestamp, written via `completeMergeMetadataFn` → `bead.MergeMetadata` (`internal/bead/bdcli.go:263-266`). (c) a refutation clears ONLY canonical `REQUEST_CHANGES` — never a `REJECT`, never a `hard_block: true`, never an unrecognized verdict, and never any non-vote condition (staleness, incompleteness, round mismatch, dirty tree, sub-threshold floor); (d) one refutation clears exactly the one named slot at the named round — no wildcard, no whole-panel form, and a newer-round re-RC of the same slot is not cleared; (e) the refutation procedure is documented in `plugins/mindspec/skills/ms-panel-tally/SKILL.md` beside the abandon procedure (line 84), with the same "do NOT refute to dodge a finding you have not actually disproven; legitimate precisely because always audited, never silent" framing abandonment carries — and crisply that a refutation's **reason + evidence carry its legitimacy**: an empty `reason` still clears (parity with the accepted empty-`abandon_reason` footgun, ADR-0037 §8), but that is a documented footgun, not an endorsed use. The trust boundary is unchanged: the record is agent-writable by design (ADR-0037 §8), legitimate because audited. **Falsified if**: recording a `refutations` entry for the sole unresolved-RC slot at the latest round does NOT unblock `mindspec complete` (all else clean); if an applied refutation lets `mindspec complete` finish successfully WITHOUT the `panel_refuted` bead-metadata entry being durably written (a swallowed metadata-write failure yielding a silent, un-audited gate-clearing completion); if the audit records a refutation that did NOT actually clear a latest-round `REQUEST_CHANGES` (stale/invalid/unused); if a refutation can clear a REJECT, a hard_block, an unrecognized verdict, or any non-vote blocking condition; if it clears a slot whose latest-round verdict is not REQUEST_CHANGES; or if a single refutation entry clears more than the one named slot+round.
3. **Round-scoping and multiplicity (scenario probes).** (a) *Multiple RCs*: with two or more unresolved REQUEST_CHANGES in the latest round, each must be refuted individually; refuting one leaves the gate blocked naming the other(s). (b) *Natural resolution*: a re-panel in which the dissenting reviewer flips to APPROVE at the new round needs no refutation — the tally already reads only the filename-derived latest round (`internal/panel/tally.go:273-276`), so the old RC simply stops being tallied; this path must remain zero-ceremony. (c) *Refute-then-re-RC*: a refutation is bound to its recorded round — if the reviewer files REQUEST_CHANGES again at a newer round, the earlier refutation does NOT carry forward and the gate blocks again until that new RC is refuted or resolved. **Falsified if**: refuting one of two unresolved RC slots unblocks the gate; if a re-panel whose latest round is all-APPROVE still requires a refutation record for a previous round's RC; or if a refutation recorded against round N clears the same slot's REQUEST_CHANGES filed at round N+1.

## Scope

### In Scope
- `internal/panel/gate.go` — the new unresolved-RC blocking condition in `PanelGateDecision`, its Block message (slot-naming, HC-7, fence), and the refutation facts' plumbing through `GateFacts`/`ResolveGateFacts`.
- `internal/panel/tally.go` — surfacing per-slot RC state and refutation coverage in `Result`; `VoteDecision` lockstep change.
- `internal/panel/panel.go` — the refutation record schema (per OQ2's resolution), parse-lenient like `abandon_reason`.
- `internal/complete/panel_advisory.go` (+ `complete.go` step 5.6/2.25 wiring) — the **durable** `panel_refuted` audit write, keyed off the gate's actually-applied refutations plumbed out of `panelGate`/`PanelGateDecision`; unlike the best-effort `panel_abandoned`/`panel_gate_skipped` writes, a metadata-write failure on an applied refutation must fail completion, not warn-and-continue.
- `plugins/mindspec/skills/ms-panel-tally/SKILL.md` (and any mirrored skill copies) — the documented refutation procedure.
- `cmd/mindspec/panel.go` — the verbs inherit the decision change (OQ3 resolved to hand-edit: no new CLI recording verb in this spec).
- `.mindspec/adr/ADR-0037-panel-gate-enforced-contract.md` — the amendment (§1 `refutations` schema addition + §3 rule extension + §7 new escape; §6/§8 re-affirmed).
- **RC-tolerance fixtures that MUST change semantically (R1's intended effect, in-scope updates)** — `internal/panel/panel_decision_test.go` (the "5 APPROVE + 1 dissent → Allow" case, ~line 184-193), `internal/complete/panel_advisory_test.go` (`TestPanelAdvisory_Passing_WouldPass`, 2 APPROVE + 1 REQUEST_CHANGES → "would PASS", ~line 68-82), and `cmd/mindspec/panel_test.go` (the "passing (at-threshold) → exit 0" case that carries an implicit dissent, ~line 386-389): each currently pins the exact RC-tolerated pass R1 removes and must be updated to the new Block/would-BLOCK expectation.
- Tests across `internal/panel`, `internal/complete`, `cmd/mindspec` pinning R1-R3 and every new AC below.

### Out of Scope
- `internal/bead` — `MergeMetadata` is consumed as-is through the existing `completeMergeMetadataFn` seam.
- `internal/config` — no new config keys; per-gate reviewer counts and thresholds (spec 112) and the recorded `approve_threshold` interpreter (spec 109, `panel.go:113-127`) are read, not changed.
- The retired PreToolUse hook (removed by spec 102), `MINDSPEC_SKIP_PANEL`, the `enforcement.panel_gate` toggle, and the abandonment procedure — all byte-unchanged.

## Non-Goals

- **Not changing per-gate reviewer counts or threshold configuration** — that is spec 112's per-gate config and spec 109's recorded `approve_threshold`; this spec only adds a blocking condition on top of whatever threshold those resolve.
- **Not changing bead-panel mechanics or the final-review flow** — panel creation, round derivation, verdict-file conventions, `panel.ForBead`'s non-bead exclusion, and the /ms-panel-run//ms-spec-final-review choreography are untouched.
- **No auto-refuting** — a refutation is always an explicit, evidenced, audited record; no code path may synthesize one (not from a fix-commit, not from a tally heuristic, not from an orchestrator default).
- **No `mindspec panel refute` CLI verb in this spec (OQ3)** — the recording surface is a hand-edit of `panel.json`'s `refutations` array, exactly parallel to the abandon procedure; the gate (`internal/panel` + `internal/complete`) carries the whole validation burden. A dedicated `mindspec panel refute <slug> --slot --round --reason --evidence` verb in the spec-110 panel-verb family (which would validate at record time) is a deliberate follow-up, not part of this spec.
- **Not touching `MINDSPEC_SKIP_PANEL`** — the human-only env hatch (ADR-0037 §7 spawn-topology rationale) keeps its exact semantics, its HC-7 never-printed rule, and its `panel_gate_skipped` audit.
- **No tamper-resistance** — per ADR-0037 §8 the refutation record is deliberately a plain agent-writable artifact; signing/hashing it is explicitly out.

## Acceptance Criteria

- [ ] **AC1 (R1 — the out-vote is closed)**: a table-driven `internal/panel` decision test (e.g. `TestPanelGateDecision_UnresolvedRequestChangesBlocks`) proves: 6 expected reviewers, complete latest-round verdicts, fresh SHA, clean tree, 5 APPROVE + 1 REQUEST_CHANGES, no refutation → Block, message names the RC slot and ends with a `recovery:` line; and an end-to-end `internal/complete` gate test (e.g. `TestPanelGate_RequestChangesBlocksComplete`, alongside the existing `TestPanelGate_*` suite) proves `complete.Run` returns the guard failure having mutated nothing. Proof: `go test ./internal/panel -run 'PanelGateDecision' && go test ./internal/complete -run 'PanelGate'`.
- [ ] **AC2 (R2 — refutation unblocks + audit persists)**: with the same 5+1 panel plus a recorded refutation naming that slot and round, `PanelGateDecision` → Allow, `mindspec complete` proceeds, and on success bead metadata carries the `panel_refuted` audit entry (applied slot, round, reason, timestamp) — pinned by a `panel_refuted` audit test beside `TestWritePanelAuditMetadata_Abandoned` (`internal/complete/panel_advisory_test.go:109-133`) plus a gate-side Allow test. Proof: `go test ./internal/complete -run 'PanelRefuted|PanelGate' && go test ./internal/panel -run 'Refut'`.
- [ ] **AC3 (R2c/R2d — refutation scope)**: tests prove a refutation recorded against a REJECT slot or a hard_block slot does NOT unblock (leg 9 fires unchanged), and refuting slot A while slot B holds an unresolved RC still Blocks naming slot B. Proof: `go test ./internal/panel -run 'Refut'`.
- [ ] **AC4 (R3 — round-scoping)**: tests prove (a) a latest-round all-APPROVE re-panel passes with no refutation despite a prior round's RC, and (b) a refutation recorded for round N does not clear the same slot's round-N+1 RC. Proof: `go test ./internal/panel -run 'Refut|Round'`.
- [ ] **AC5 (unchanged paths — with the RC-tolerance carve-out)**: every pre-existing panel-gate behavior EXCEPT the RC-tolerated pass is preserved — skip-env Warn + `panel_gate_skipped` audit, abandoned Warn + `panel_abandoned` audit, stale-SHA Block, incomplete Block, round-mismatch Block, dirty-tree Block, missing-ref/transient-gitErr Warns, fail-open with no panel — with those `internal/panel`/`internal/complete`/`internal/instruct`/`cmd/mindspec` tests passing without semantic modification (mechanical fixture additions only where a struct gains fields). **Carve-out (the one intended semantic break, R1's whole point)**: the three RC-tolerance fixtures named in In Scope — `internal/panel/panel_decision_test.go`'s "5 APPROVE + 1 dissent → Allow", `internal/complete/panel_advisory_test.go`'s `TestPanelAdvisory_Passing_WouldPass` (2 APPROVE + 1 REQUEST_CHANGES → "would PASS"), and `cmd/mindspec/panel_test.go`'s "passing (at-threshold) → exit 0" dissent case — WILL be updated to the new Block / would-BLOCK / non-zero expectation; "no semantic modification" applies to all OTHER tests, and no fixture outside this named set flips outcome. Proof: `go build ./... && go test ./internal/panel ./internal/complete ./internal/instruct ./cmd/mindspec`, plus a diff review confirming only the three named fixtures changed expected outcome.
- [ ] **AC6 (ADR + skill doc)**: ADR-0037 carries the dated amendment enumerating §1 (`refutations` schema), §3 (threshold extension), §7 (durable refutation escape + no-advertise), and re-affirming §6/§8; `ms-panel-tally/SKILL.md` documents the refutation procedure beside the abandon procedure, including that reason+evidence carry its legitimacy (empty-reason-still-clears footgun). Proof: `grep -n 'refut' .mindspec/adr/ADR-0037-panel-gate-enforced-contract.md plugins/mindspec/skills/ms-panel-tally/SKILL.md` returns hits in both.
- [ ] **AC7 (R1/OQ1 — sub-threshold-after-refutation STILL blocks)**: a decision test proves a panel with 6 APPROVE + 2 REQUEST_CHANGES at `expected_reviewers` 8 / threshold 7, both RCs refuted, complete + fresh SHA + clean tree → **Block** on the approval floor (6/8 < 7), message reflecting sub-threshold not RC — proving refutation cannot buy past the floor. Proof: `go test ./internal/panel -run 'Refut|PanelGateDecision'`.
- [ ] **AC8 (R1/R2c — unrecognized latest-round verdict BLOCKS and is NON-refutable)**: a decision test proves 5 APPROVE + 1 unrecognized/non-standard verdict (threshold 5, complete, fresh SHA) → **Block** (the unrecognized verdict is "not an APPROVE"), and that a `refutations` entry naming that slot still → **Block** (refutation clears only canonical `REQUEST_CHANGES`). Proof: `go test ./internal/panel -run 'Refut|PanelGateDecision'`.
- [ ] **AC9 (R1/OQ1 — a refuted RC never increments `Approves`)**: a test asserts that applying a refutation leaves `Result.Approves` unchanged (the cleared slot is removed from the blocking set, never re-counted as APPROVE), so the threshold floor is computed over genuine approvals only. Proof: `go test ./internal/panel -run 'Refut'`.
- [ ] **AC10 (R1/OQ4 — no-advertise Block-message predicate)**: a fixed-string test on the unresolved-RC Block message asserts it (i) names the unresolved slot id(s), (ii) ends with an ADR-0035 `recovery:` line, and (iii) contains NO paste-able refutation incantation — no `panel refute`, no `refutations`, no `"refute"` substring — mirroring the existing HC-7 assertion that the block never prints `MINDSPEC_SKIP_PANEL`. Proof: `go test ./internal/panel -run 'PanelGateDecision' && go test ./internal/complete -run 'PanelGate'`.
- [ ] **AC11 (R2/G2 — durable audit: a write failure must NOT silently complete)**: a test injecting a `completeMergeMetadataFn`/`bead.MergeMetadata` failure on an APPLIED refutation proves `mindspec complete` returns non-zero (or otherwise fails the completion) rather than merging with the gate silently cleared and no `panel_refuted` recorded — distinguishing this durable path from the best-effort `panel_abandoned`/`panel_gate_skipped` writes, which still warn-and-continue. Proof: `go test ./internal/complete -run 'PanelRefuted|PanelGate'`.
- [ ] **AC12 (R2 test-note — duplicate slot files at latest round)**: the plan/tests must state the chosen behavior for the pre-existing ambiguity of two verdict files sharing a `slot` at the latest round (e.g. `x-round-2.json` twice under different casing, or a re-run leaving both) as it interacts with a refutation naming that slot — a test pins that a refutation resolves against the slot's tallied latest-round verdict deterministically and does not clear a REJECT hiding under a duplicate. Proof: `go test ./internal/panel -run 'Refut|Dup'`.

## Validation Proofs

- `go build ./... && go test ./internal/panel ./internal/complete ./internal/instruct ./cmd/mindspec`: all PASS; only the three named RC-tolerance fixtures changed outcome, no other test semantically modified (AC5 carve-out).
- `go test ./internal/panel -run 'PanelGateDecision|Refut|Round|Dup'`: AC1/AC3/AC4/AC7/AC8/AC9/AC12 decision-table cases green, including the 5-APPROVE+1-RC Block, the refuted-RC Allow, the sub-threshold-after-refutation Block, and the unrecognized-verdict non-refutable Block.
- `go test ./internal/complete -run 'PanelGate|PanelRefuted'`: AC1's end-to-end Block, AC2's `panel_refuted` audit write, AC10's no-advertise message predicate, AC11's durable-audit write-failure fence, and the unchanged skip/abandon audits green.
- `grep -n 'refut' .mindspec/adr/ADR-0037-panel-gate-enforced-contract.md plugins/mindspec/skills/ms-panel-tally/SKILL.md`: AC6 hits in both files.
- `mindspec validate spec 114-panel-gate-block-on-request-changes`: passes (advisory WARN acceptable).

## Open Questions

- [x] **OQ1 (do refuted RCs shrink/satisfy the threshold floor?)** grill (self-answered, headless): → **NO — layered model.** The `approve_threshold`/N−1 floor stays a necessary condition on the count of genuine `APPROVE` verdicts; a refuted RC is removed from the blocking set but never increments `Approves`, so an at-threshold panel unblocks on refutation while a sub-threshold panel (e.g. 6 APPROVE + 2 refuted RC, threshold 7) STILL blocks on the floor. Rationale: conservative + hard-to-game (you cannot refute past the approval floor) and it keeps `approve_threshold` meaningful (ADR-0040). Encoded in R1 (chosen-model statement + sub-threshold falsifier).
- [x] **OQ2 (where the refutation record lives)** grill (self-answered, headless): → **a `refutations` array on `panel.json`** (entries `{slot, round, reason, evidence}`), NOT per-slot files, mirroring the abandon schema (`internal/panel/panel.go:58-68`); plus the mandatory `panel_refuted` bead-metadata audit entry at complete (`writePanelAuditMetadata`, `internal/complete/panel_advisory.go:304-342`). Rationale: one file, parse-lenient, ADR-0037 §1 schema amendment; no second filename grammar in `Tally`. Encoded in R2 (exact schema).
- [x] **OQ3 (recording surface)** grill (self-answered, headless): → **hand-edit `panel.json`, gate-validated; a `mindspec panel refute` verb is an explicit follow-up NON-GOAL.** A plain repo-file edit is legitimate precisely because always audited (same justification the ms-panel-tally skill gives for abandon being agent-performable); the validation burden lives in the gate (`internal/panel` + `internal/complete`), which clears only a slot whose latest-round verdict is REQUEST_CHANGES and never a REJECT/hard_block/incomplete/stale/sub-threshold. Encoded in R2 and Non-Goals.
- [x] **OQ4 (may the Block message advertise the refutation procedure?)** grill (self-answered, headless): → **NO.** The Block names the unresolved RC slot(s) + an ADR-0035 `recovery:` line advising fix-and-re-panel as the primary path; it does NOT print a paste-able refutation incantation — exactly as the gate never prints a paste-able `MINDSPEC_SKIP_PANEL` prefix. The escape is discoverable in ADR-0037 §7 + the /ms-panel-tally skill, not reflex-advertised, so refutation stays a deliberate looked-up action. Encoded in R1 (message format) + the ADR-0037 amendment.

## Approval

- **Status**: APPROVED
- **Approved By**: user
- **Approval Date**: 2026-07-10
- **Notes**: Approved via mindspec approve spec