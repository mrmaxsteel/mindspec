# spec-114-final — Round 2 Final Review (12 reviewers) — round-1 fix re-verify

**Branch**: `spec/114-panel-gate-block-on-request-changes` @ **2b4fb8f09eff6e008b5887c1a765c3339874c880** (round-1 was `41d8e8af`). **Base**: `origin/main` `71398a68` (NOTE: the /tmp clone's local `origin/main` ref is STALE at `ef58cf8b`, an ancestor — diff against `71398a68` explicitly, as round 1 did).
**Panel**: 12 slots — F1–F3 Fable, O1–O3 Opus, S1–S3 Sonnet, G1–G3 codex. **Pass = ≥11 APPROVE, no REJECT.** Findings adjudicated, never out-voted.

**READ `BRIEF.md`** (same dir) for the full spec context. This round re-verifies the round-1 fixes — the round-1 delta is **DOC-ONLY**.

**READ-ONLY RULE (HARDENED)**: ALL build/test in an ABSOLUTE /tmp clone; NEVER edit the live worktree. Pin `2b4fb8f0`. Verdict ONLY to the absolute path at bottom. git clean.

## Round-1 result + what changed (the entire round-2 delta — 2 doc files, NO code)
Round 1 was 8 APPROVE / 4 REQUEST_CHANGES. The two findings, both now adjudicated. `git diff 41d8e8af 2b4fb8f0` = ONLY `spec.md` + `plan.md` (doc addenda) + the round-1 review artifacts. **Zero `.go` changes** → the CI-green result from round 1 (build/gofmt-1.23/vet/full test/-race, all confirmed by G1/S1/O1/F1/F2/S2/O2/O3) still holds by construction.

### Finding A — spec.md still described the DELETED discharge mechanism (raised by G3, S3, F3; noted by O3, F2, F1) → FIXED
spec.md's §7 (line ~39 "a `refutation_discharged` audit"), R1/R2 (satisfy/discharge/refuse trichotomy, "verified-discharged"), In Scope (line ~56 "RE-TALLYING the matched panel … `panel.Tally`"), AC11(d)/(e)/(f), and the R2 falsifiers still described the discharge-by-re-tally path that Bead 2 DELETED. Because ADR-0023 is forward-only (the approved spec text is not rewritten), the fix is a **dated superseding ADDENDUM** at the top of spec.md ("## Amendment (2026-07-10, implementation — DISCHARGE PATH DELETED …)") that explicitly supersedes every discharge/`refutation_discharged`/verified-discharge/re-tally reference below and states the shipped design (self-contained marker → always satisfy `panel_refuted` from the marker, every path incl hatch, no re-tally, no discharge; natural resolution = no marker = zero ceremony). The plan's DESIGN AMENDMENT was also broadened spec-wide (F3-2) and the AC2 provenance label fixed (O3 nit).
**Verify**: (1) the spec.md addendum accurately describes the SHIPPED design (matches the code + ADR + consolidated-round-3.md) and unambiguously supersedes the stale discharge passages — a reader can no longer mistake spec.md as claiming discharge is current. (2) It is forward-only-compliant (append/supersede, not a rewrite of approved text). (3) The plan broadening + provenance-label fix are correct. Is Finding A genuinely resolved?

### Finding B — impl-approve/FinalizeEpic auto-merges raw-`bd close`'d beads un-gated (raised by G2 codex 0.99, CONFIRMED real at `internal/executor/mindspec_executor.go:381-418`) → ADJUDICATED as out-of-scope follow-up
`mindspec impl approve` → `FinalizeEpic` auto-merges unmerged bead branches closed via raw `bd close` (skipping `complete`'s panel gate) — a real bypass of the same silent-out-vote class, but on a DIFFERENT verb than spec 114's `mindspec complete` scope (R1/R2 are explicitly `complete`-scoped; this path is pre-existing and already an orchestrator-forbidden anti-pattern). Disposition: filed as follow-up **`mindspec-o4fd` (P1)** (harden FinalizeEpic to run the same panel-gate + durable-refutation reconciliation before auto-merge) + a dated **"## Scope note"** added to spec.md stating 114 enforces the `complete` gate and o4fd is the tracked follow-up. Both dedicated anti-gaming reviewers (O1 end-to-end, F1 composition) confirmed no un-audited path *within* 114's mechanism.
**Verify (the consequential call — assess it):** is treating Finding B as an out-of-scope, tracked follow-up (rather than a 114 blocker) the RIGHT disposition? Consider: 114 scoped R1/R2 to `complete`; B is a pre-existing separate-verb path; O1/F1 cleared 114's own mechanism; the scope note is honest (doesn't over-claim 114 closed the impl-approve path); o4fd is filed P1. If you believe B MUST block 114's merge to main instead, say so explicitly with reasoning (this is the one open judgment call — the panel decides it). If you accept the follow-up disposition + honest scope note, APPROVE on this axis.

## What to verify (round 2)
1. **Finding A resolved (all, esp. G3/S3/F3)** — spec.md addendum accurate + forward-only + unambiguously supersedes the discharge language; plan broadening + label fix correct.
2. **Finding B disposition (all, esp. G2)** — is out-of-scope-follow-up + honest scope note + o4fd(P1) acceptable, or a hard blocker? Decide + state reasoning.
3. **No code regression (G1/S1)** — confirm `git diff 41d8e8af 2b4fb8f0` touches no `.go` file; a quick /tmp `go build ./... && gofmt -l ./cmd ./internal && go test ./internal/panel ./internal/complete ./internal/bead ./cmd/mindspec` re-confirms CI-green (it must be, since no code changed).
4. **Everything round-1-APPROVED still holds** — the mechanism, ACs, anti-gaming, scope: all unchanged from round 1 (only docs moved). Carry your round-1 verdict forward unless the doc addenda introduced a NEW problem (e.g. an inaccurate addendum, an over-claim, an internal contradiction).

Verdict: APPROVE / REQUEST_CHANGES / REJECT. Output JSON to `<this-dir>/<your-slot>-round-2.json`: `reviewer_id`, `verdict`, `confidence`, `rationale` (≤200 words), `concrete_changes_required` (empty if APPROVE), `findings` (mark round-1 findings ADDRESSED/OPEN).
