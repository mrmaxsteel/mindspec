# spec-114-final — Final Review Panel (12 reviewers, four families) — whole spec vs main

**Worktree**: `/Users/Max/replit/mindspec/.worktrees/worktree-spec-114-panel-gate-block-on-request-changes`
**Branch**: `spec/114-panel-gate-block-on-request-changes` @ **41d8e8afce90ba3d3bf6f96c3bdd7b9b8221332d**. **Base**: `origin/main` (`71398a68`).
**Panel**: 12 slots — F1–F3 Fable, O1–O3 Opus, S1–S3 Sonnet, **G1–G3 codex (real gpt-5.6-sol)**. **Pass = ≥11 APPROVE, no REJECT.** Every finding adjudicated (fixed or evidenced-refuted), never out-voted.

**READ-ONLY RULE (HARDENED)**: ALL build/test work in an ABSOLUTE `/tmp` copy (`git archive`/clone), NEVER the live worktree. Pin reads to `41d8e8af`. Write your verdict ONLY to the absolute path at the bottom. Leave `git status` clean.

## What spec 114 ships (the whole thing, as ONE PR to main)
Closes the last silent out-vote in `mindspec complete`'s panel gate. Three merged beads:
- **Bead 1 — leg 9.5**: any unresolved REQUEST_CHANGES / unrecognized verdict blocks `complete`, LAYERED on the existing threshold floor (after REJECT/hard_block, before the threshold Allow). `VoteDecision` twin in lockstep. `internal/panel/gate.go` + `tally.go`.
- **Bead 2 — audited-refutation escape + durable obligation**: an operator clears ONE slot's RC by hand-editing `panel.json`'s `refutations` array `{slot,round,reason,evidence}`; the GATE validates (clears only that slot's latest-round RC; never REJECT/hard_block/unrecognized; `Approves` never incremented → can't buy past the floor; AC12 byte-exact+dedup). A **self-contained `refutation_pending` marker** is durably persisted as part of the allow-decision (union not replace; read-or-write fail ⟹ Block); reconciliation writes the honest **`panel_refuted`** audit FROM the marker on every path incl. hatch, before close — **NO discharge, NO re-tally** (the discharge-by-re-tally path was designed, then DELETED after 3 bead-panel rounds found it spoofable — operator decision). Fail-closed `MergeMetadata`/`GetMetadata`. `internal/panel/panel.go` + `internal/complete/*` + `internal/bead/bdcli.go`.
- **Bead 3 — docs**: ADR-0037 amendment (§1/§3/§6/§7/§8) + refutation procedure in both `ms-panel-tally` SKILL copies.

**Governing artifacts**: `.mindspec/specs/114-panel-gate-block-on-request-changes/spec.md` (R1/R2, AC1–AC12), `plan.md` (amended: Bead-2 DESIGN AMENDMENT + AC11 record the no-discharge design), `reviews/spec-114-bead2/consolidated-round-3.md` (the discharge-deletion decision).

## What to verify (this is the HOLISTIC final gate — the beads already passed individually; find INTEGRATION + whole-spec issues)
1. **The three beads COMPOSE (F1/O1/G2)** — leg 9.5 (Bead 1) + the refutation clear (Bead 2) + the docs (Bead 3) form one coherent mechanism. Trace end-to-end: an unresolved RC blocks (9.5); a valid `refutations` entry clears exactly that slot's latest-round RC and persists a self-contained marker; `complete` writes the durable `panel_refuted` before merge; a sub-threshold panel still blocks even with all RCs refuted (floor intact). No seam bug between beads.
2. **Every spec AC is actually satisfied (O3/G3/F2)** — walk AC1–AC12 in `spec.md` against the shipped code + a NAMED passing test each. AC11's language is the amended no-discharge form (satisfy-from-marker; the plan's AC11 row + Bead-2 DESIGN AMENDMENT reflect this). Any AC not truly met = finding.
3. **Anti-gaming is airtight end-to-end (O1/O2/G2 — the spec's whole point)** — a reviewer's REQUEST_CHANGES can NEVER be silently out-voted: the only escapes are (a) an audited per-slot refutation (durable `panel_refuted`, never silent), (b) the human-only `MINDSPEC_SKIP_PANEL` env hatch (audited `panel_gate_skipped`), (c) whole-panel abandon (audited `panel_abandoned`). Confirm no un-audited path clears/merges an unresolved RC. The refutation can't clear a REJECT/hard_block/unrecognized/other-slot/newer-round; can't increment Approves; the durable obligation survives retries/hatches/panel-removal.
4. **CI-readiness (G1/G3/S1 — empirical, the gate to merge)** — in a /tmp clone: `go build ./...`; **`gofmt -l ./cmd ./internal` MUST be empty** (CI runs go 1.23.0 gofmt — the known doc-comment backtick-shell-escape gotcha bit this repo before; check gate.go/tally.go/panel_advisory.go/bdcli.go have no such spans); `go vet ./...`; `go test ./...` (the full suite; note `internal/instruct` `TestRun_IdleNoBeads` = pre-existing z4ps flake, and `internal/harness` LLM tests spawn a real `claude` CLI + hang/need network — both environmental, not spec-114; run the spec-114 packages `./internal/panel ./internal/complete ./internal/bead ./cmd/mindspec ./internal/instruct` explicitly + `-race`). Report actual output. A real red test or gofmt failure = blocking (this must be CI-green before merge to main).
5. **Doc/plan/ADR/spec COHERENCE (O3/F3/G3)** — the plan describes the SHIPPED design (Bead-2 DESIGN AMENDMENT + AC11 record the discharge deletion — the plan's original discharge text is superseded, not contradictory); ADR-0037's amendments match the code; spec.md's R1/R2 match. No artifact still claims a discharge mechanism the code lacks (grep the shipped ADR/SKILL — only the "written from a self-contained marker, not by re-reading panels" rationale is acceptable). The `internal/bead` execution-domain edit is declared in spec.md Impacted Domains.
6. **No regression to `mindspec complete`/gate/instruct (O2/S2/G2)** — every PRE-EXISTING gate behavior is byte-preserved except the intended change: REJECT/hard_block/incomplete/stale-SHA/round-mismatch/dirty-tree/abandon/skip all still block/warn as before; the `panelGate` signature evolution + `Decision` struct change are contained; `cmd/mindspec panel verify/tally` + `instruct --panel-state` still delegate to the same decision.
7. **Scope + provenance + no stray artifacts (S3/F2)** — the substantive delta (excluding `reviews/`) is the ~23 files listed in the diff (internal/panel|complete|bead + cmd/mindspec + internal/instruct test + ADR + 2 SKILL + spec.md + plan.md + .beads). No unexpected file, no leftover scratch, no debug code. The AC5 fixture reconciliation across all beads holds (no unclassified outcome flip).

## Per-slot lens defaults
- **F1** bead-composition / end-to-end trace; **F2** grounding (every claim ↔ real code) + scope; **F3** contradiction (spec vs plan vs ADR vs code; the no-discharge coherence).
- **O1** anti-gaming end-to-end + refutation soundness; **O2** no-regression to the gate/complete/instruct; **O3** AC provenance (AC1–AC12 each → named test) + doc coherence.
- **S1** CI-readiness empirical (build/gofmt/vet/test/-race); **S2** no-regression pin (existing tests green, fixtures classified); **S3** scope fence + stray-artifact check.
- **G1** empirical CI-matrix prover (build/gofmt-1.23/vet/full test); **G2** anti-gaming + durable-obligation feasibility (any un-audited-merge path in the WHOLE mechanism?); **G3** completeness/AC-coverage + doc-vs-code accuracy.

Verdict: APPROVE / REQUEST_CHANGES / REJECT. Output JSON to `<this-dir>/<your-slot>-round-1.json`: `reviewer_id` ("<slot> <family>"), `verdict`, `confidence` (0–1), `rationale` (≤200 words), `concrete_changes_required` (empty if APPROVE), `findings` (set `"hard_block": true` for a missing measurement artifact / un-audited-merge path / CI-red).
