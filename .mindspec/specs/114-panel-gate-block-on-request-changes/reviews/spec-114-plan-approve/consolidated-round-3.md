# spec-114-plan-approve — Round 3: 4+ RC (O1, G1, G2, G3) / APPROVE (O2, O3, F1, F2?, F3?)

The durable-obligation (step 2.3 marker + fail-open override) CLOSED the round-2 panel_refuted-write hole, but the sharp slots (gpt-5.6-sol 0.99 + O1) found it MOVED the hole one step earlier + left a path gap. Three narrow, cleanly-closeable residuals + editorial.

## Residuals (round-4 fixes)
1. **[O1#1/G2/G1] GetMetadata read-error not fail-closed.** The no-panel obligation override reads bead.GetMetadata; the plan never specifies read/parse-error behavior. A transient read failure must return a guard failure (REFUSE merge) — NOT fail-open. Fix + e2e test asserting closeBeadFn + ex.completeCalled stay false on a GetMetadata error.
2. **[O1#2/G2 — REDESIGN, closes it] "Applied" = durably-persisted.** Today panelGate applies the refutation in-memory, THEN step 2.3 writes the marker; a marker-write-failure aborts with the refutation "applied" in memory but no durable obligation → panel-removed retry fail-opens (invariant fails one step earlier). FIX (G2's requested redesign): define a refutation as "applied" (clears the RC) ONLY after its pending-marker is durably persisted. A marker-write-failure means the refutation is NOT applied → the RC is NOT cleared → the gate BLOCKS (returns a Block, bead stays in_progress with the RC, no merge), not an abort-with-applied. Then "applied ⟹ durable obligation" is definitional; a panel-removed pristine bead (marker never persisted, RC never cleared) is the pre-existing §6 no-panel fail-open of a bead on which NO refutation ever took durable effect — NOT a refutation-specific un-audited merge. Add a cross-run test: marker-write-fails → RC still blocks (not merge); and the pristine-panel-removed case reasoned/tested as the §6 boundary.
3. **[G3] Reconcile obligation on EVERY completion path.** The obligation check is only on the no-panel fail-open path. A LIVE-panel retry where the reviewer flipped RC→APPROVE (natural resolution) has no applied refutation in run-2 but STILL carries the unsatisfied refutation_pending from run-1 → it bypasses the no-panel check → merges with the recorded refutation un-audited. FIX: on EVERY completion path (panel-present AND no-panel), before close, an unsatisfied refutation_pending must be reconciled — satisfied (write panel_refuted for the recorded pending) OR explicitly discharged (record a discharge audit if the RC resolved naturally so the refutation is moot) — never silently ignored. Define the discharge-vs-satisfy rule. Add a cross-run test: run 1 records pending + fails panel_refuted; run 2 has a live all-APPROVE panel → must REFUSE/reconcile until the prior obligation is audited, not silently merge. Cover the skip-env path too.

## Editorial (O3/G1/G3/F1/O2)
4. spec.md:65 Out-of-Scope "internal/bead — MergeMetadata consumed as-is" CONTRADICTS In Scope + Impacted Domains — strike/rewrite (O3, G1, G3 all flag).
5. spec.md ADR Touchpoints + AC6 must require the narrow operator-authorized ADR-0037 §8 durable-audit carve-out (plan.md:766-787), stating the general anti-adversary posture is unchanged (G1).
6. Correct plan.md:496-497 fail-open citation complete.go:152-156 → panel_advisory.go:153-155, caller insertion at complete.go:375-378 (G1); the step-5(c) stale ref (F1); name the new test Go functions under Refut/PanelRefuted prefixes (F1); the GetMetadata harness seam coherent with completeMergeMetadataFn (F1); soften the "anti-tamper" label to match the delivered audit-durability guarantee (O2).

Net after round-4: applied⟹durable-marker (definitional), obligation reconciled on every path, GetMetadata fail-closed → the invariant "a refutation that ever DURABLY clears an RC ⟹ audited across all retries" is airtight; the only fail-open is a bead on which no refutation ever took durable effect (§6/§8 boundary, documented).
