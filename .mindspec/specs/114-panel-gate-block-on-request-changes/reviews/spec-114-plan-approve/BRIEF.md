# spec-114-plan-approve — Plan-Approval Panel (9 reviewers, three families)

**Worktree**: `/Users/Max/replit/mindspec/.worktrees/worktree-spec-114-panel-gate-block-on-request-changes`
**Branch**: `spec/114-panel-gate-block-on-request-changes` @ **bc6d35d7c10d162a0bce505770d743de3422d894**
**Under review**: `.mindspec/specs/114-panel-gate-block-on-request-changes/plan.md` (DRAFT plan). Read `spec.md` (same dir) for R1/R2/AC1-AC12. Base = origin/main (specs 109-113).
**Panel**: 9 slots — F1–F3 Fable, O1–O3 Opus, **G1–G3 codex (real codex)**. **Pass = ≥8 APPROVE, no REJECT. Every finding adjudicated (fixed or evidenced-refuted), never out-voted.**

**READ-ONLY RULE (MANDATORY)**: verdict JSON only; pin reads to `bc6d35d7`; scratch under ABSOLUTE /tmp only; leave `git status` clean. Verdicts ONLY to the exact absolute path at the bottom.

## What this plan implements (the approved spec 114)
`mindspec complete`'s panel gate must BLOCK on any unresolved REQUEST_CHANGES (not just REJECT), with an audited-refutation escape. The spec-approval panel (9/9) confirmed the design is anti-gaming-airtight-by-construction. Plan = **3 beads, serial A→B→C**:
- **Bead 1 (R1 gate-block)** — new leg **9.5** in `internal/panel/gate.go` `PanelGateDecision` (after leg 9 REJECT/hard_block, BEFORE leg 10 threshold-Allow) that blocks an unresolved RC / unrecognized latest-round verdict; the `VoteDecision` lockstep twin; the AC5 fixture reconciliation. Layered model: threshold stays a NECESSARY floor on genuine `Approves`; a refuted RC clears the veto but NEVER increments `Approves`.
- **Bead 2 (R2 refutation + DURABLE audit)** — `Refutation` schema on `panel.json` (`{slot,round,reason,evidence}`, parse-lenient); gate validation single-homed (`Result.UnresolvedVerdicts()`/`AppliedRefutations()`): a refutation clears ONLY a slot whose latest-round verdict is REQUEST_CHANGES (never REJECT/hard_block/incomplete/stale/sub-threshold/unrecognized), exactly one slot+round; applied refutations plumbed via a new `Decision.AppliedRefutations` field → `panelGate` signature change; **DURABLE `panel_refuted` audit** at new `complete.Run` **step 4.75** (write-before-merge).
- **Bead 3 (docs)** — ADR-0037 amendment (§1/§3/§7 + §6/§8 re-affirm) + refutation procedure in BOTH `ms-panel-tally` SKILL.md copies (plugins + .claude mirror).

## What to verify (this is a PLAN panel — ground every claim at bc6d35d7; the subtle parts are flagged)
1. **Durable-audit ORDERING (O1/G2 — the load-bearing design)** — Bead 2 puts the `panel_refuted` write at a NEW `complete.Run` step 4.75: AFTER step-4 close + forced dolt-commit + committed-state verify (`internal/complete/complete.go:546-650`), BEFORE the terminal `exec.CompleteBead` merge (`complete.go:667-672`), via a NON-swallowing `writePanelRefutedMetadata` that returns the `MergeMetadata` error → a failure returns a pre-merge `guard.NewFailure`. VERIFY: (a) this genuinely makes AC11 hold (the merge can NEVER land un-audited when a refutation was applied); (b) the failure lands in the documented closed-but-unmerged recoverable state (`complete.go:695-708` convergence contract) — is that recoverable state acceptable / does the plan handle it? (c) the best-effort `writePanelAuditMetadata` (step 5.6, abandon/skip) stays byte-unchanged (the asymmetry is intentional: abandon/skip are terminal non-merge exits; only refutation changes the merge outcome). Any way the audit could be lost while the merge lands = REQUEST_CHANGES.
2. **`Decision` struct change + panelGate signature (O1/G2/O3)** — `Decision` gains an `AppliedRefutations` slice → the `panelGate` signature changes + one `Decision != Decision` struct comparison (panel_test.go ~254) needs a mechanical fix (slices aren't comparable with `!=`). Is the struct change contained + the comparison fix correct (reflect.DeepEqual or field-wise)? Does it stay within `internal/panel`+`internal/complete` (workflow domain)?
3. **AC5 fixture reconciliation (F3/G1/S?)** — the plan names 3 spec-cited flips (panel_decision_test.go:184-194, panel_advisory_test.go:69-81, cmd panel_test.go:386-389) + the VoteDecision twin (votedecision_test.go:64-70) as INTENDED semantic breaks, AND enumerates 5 ADDITIONAL fixtures using RC/empty-verdict FILLER incidentally (panel_test.go:224/304/393, panel_gate_e2e_test.go approveVerdicts, instruct panelstate_test.go) as outcome-PRESERVING input updates, PLUS designs the leg-9.5 message as a SUBSTRING SUPERSET of leg-10 so four sub-threshold fixtures stay byte-green. VERIFY this reconciliation is sound (does making leg-9.5's message a superset of leg-10's actually keep those assertions green? are the 5 "incidental" fixtures really outcome-preserving, or do some genuinely flip?). AC5's diff-review proof (no fixture OUTSIDE the named set flips) must be a real step.
4. **Decomposition (O3/G3)** — 3 beads, serial A→B→C (chain length 3, OK). A→B dependency real (can't clear an RC that doesn't yet block); B→C real (AC6 grep needs shipped names). A+B both touch gate.go/tally.go/internal/complete tests — the R_scope overlap: is keeping them separate justified (B builds on A's merged state, serial, no conflict) vs merge-into-one? Each bead one-round bead-panel verifiable.
5. **Provenance (O3)** — every spec AC (AC1-AC12) mapped to a bead + a NAMED runnable test. AC8/AC10 split across beads — coherent? AC11 (durable audit) + AC12 (duplicate-slot) mapped?
6. **ADR Fitness (O2)** — ADR-0037 amendment (§1 schema/§3 threshold/§7 escape/§6§8 re-affirm) correct; ADR-0035 (block-message recovery, no-advertise), ADR-0040 (threshold-as-floor), ADR-0023 (refutation = durable artifact) verdicts sound. Each linked ADR resolves.
7. **Grounding (F2/G1)** — the cited file:line (gate.go legs, complete.go steps 546-650/667-672/695-708, the fixtures, panelGate, MergeMetadata) exist + mean what the plan says. A claim the tree doesn't support = finding.
8. **Testing Strategy (O2/S?)** — the injected-MergeMetadata-failure test (AC11) drives the real durable path; the decision-table anti-gaming rows (AC7/AC8/AC9); the block-message predicate (AC10). Shared infra named.

## Per-slot lens defaults
- **F1** per-bead falsifiability + all ACs mapped; **F2** grounding (plan ↔ real code); **F3** contradiction (plan vs spec vs itself; the AC5 reconciliation coherence).
- **O1** durable-audit ordering + Decision-struct-change soundness (LOAD-BEARING); **O2** ADR fitness + testing strategy; **O3** decomposition + provenance + domain parity.
- **G1** empirical grounding (verify cited lines + the fixture states); **G2** durable-audit feasibility + the write-before-merge/recoverable-state design (does it actually prevent un-audited merge?); **G3** completeness/decomposition (beads land cleanly; the 5 incidental fixtures really outcome-preserving; anything missing).

Verdict: APPROVE / REQUEST_CHANGES / REJECT. Output JSON to `<this-dir>/<your-slot>-round-1.json`: `reviewer_id`, `verdict`, `confidence` (0–1), `rationale` (≤200 words), `concrete_changes_required` (empty if APPROVE), `findings`.
