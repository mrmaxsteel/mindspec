# spec-114-plan-approve — Round 4: 5 APPROVE / 4 RC → convergence round-5

Round-4 CLOSED the round-3 holes (O1: both ADDRESSED; G2: "one-step-earlier hole closed"; G3: natural-resolution addressed). Remaining = 3 narrow gaps in the pending-SET reconciliation. O1 (Opus) calls the mechanism "sound and complete for the reachable state space" except these; O1 calls #1 "a one-sentence internal-consistency fix." NOT infinite regress — round-5 completes the reconciliation totally.

## Round-5 fixes (complete the reconciliation)
1. **[O1+G2 — primary] UNION pending entries, reconcile ALL.** The step-2.3 marker write persists the CURRENT run's refutation_pending_entries; MergeMetadata merges at top-level key granularity → a REPLACE overwrites older unsatisfied pending. Loss path: run1 refutes X(round1), writes marker (X,1); its panel_refuted FAILS (obligation durable, bead in_progress); re-panel to round2; (X,2) or a new (B,2) OVERWRITES [(X,1)] → reconciliation satisfies only the new entry + closes → (X,1) never audited/discharged → AC11 invariant violated. FIX: the marker write MUST UNION with existing unsatisfied pending entries (never replace); reconciliation must satisfy/discharge/refuse EVERY entry in the unioned set before close. (O1: the plan's own Discharge case already presumes past-round pending is still recorded — internal-consistency.) Add a cross-run test: run1 marker (X,1) + panel_refuted-fails; run2 = X naturally-APPROVE + newly-refuted B → require durable refutation_discharged for X AND panel_refuted for B before close; any read/write failure leaves it in_progress.

2. **[G1+G3 — discharge evidence] Discharge requires VERIFIED vote-state, not "gate passed."** The discharge rule keys on "panel present + gate passed ⟹ natural resolution." But PanelGateDecision ALSO passes via Warn — abandonment (gate.go:160-169), missing-ref (180-189), transient-gitErr (192-203) — WITHOUT the RC resolving. So a carried pending could be FALSELY discharged (audited as moot) while its RC is still an unresolved latest-round REQUEST_CHANGES. FIX: discharge a pending (slot,round) ONLY on affirmative evidence from the actual tally that the slot is no longer a latest-round REQUEST_CHANGES at/after its recorded round (reviewer flipped to APPROVE, or the round advanced past it) — plumb the vote-state/tally facts from panelGate, not a bare Allow. An early-pass-Warn panel that still carries the RC must NOT discharge (refuse or keep the obligation). Tests: cross-run pending + each live-panel Warn path (abandoned/missing-ref/transient) → NOT generically discharged.

3. **[G3 — hatches] Reconcile before close on EVERY path incl. hatches.** The plan makes reconciliation a no-op for MINDSPEC_SKIP_PANEL / enforcement.panel_gate=false → a durable stale pending can merge via a hatch un-audited. The obligation-audit is INDEPENDENT of the gate skip (the hatch bypasses the GATE decision, not a pre-existing durable audit obligation). FIX: run pending-obligation reconciliation before close even on hatch paths — an existing pending must be satisfied/validly-discharged/refused, never ignored. Specify the exact hatch interaction (the hatch still allows the merge re the GATE, but the pending audit must be written; if unverifiable → refuse). Tests: stale pending + env-skip, config-disabled, abandoned-panel retries → no close/merge unless a covering durable audit is written.

## Minor (F3/O2/F1)
- Reconcile spec AC5 "three flip fixtures" vs plan's "four" (the VoteDecision twin the spec's own R1 falsifier mandates) — Bead-1-time note (F3).
- Cosmetic discharge-reason wording (F3); read-after-write comment on the same-run satisfy path (O2); the Decomposition summary still uses round-3 "fail-open-path override" wording superseded by every-path reconcile (F1).

After round-5: reconciliation is TOTAL — unioned full pending set, verified vote-state discharge, every path incl. hatches. The invariant "a refutation that ever durably clears an RC ⟹ audited (satisfied) or verified-discharged, across all retries/paths" is airtight.
