# spec-114-approve — Round 1: 5 APPROVE / 4 REQUEST_CHANGES

APPROVE: F2 0.93, G1 0.96, O1 0.87, O2 0.85, O3 0.9. RC: F1 0.78, F3 0.82, G2 0.88, G3 0.86.
Core design CONFIRMED sound by the approvers: R1 layered model composes with the legs (O1); anti-gaming airtight BY CONSTRUCTION (legs 2-9 short-circuit before the RC leg; refuted RC never touches Approves — O1/G2); grounding all cited lines hit (F2/G1); ADR-0037 amendment coherent (O2); domain parity + ~3-bead decomposition (O3). Findings are SPEC-QUALITY (AC coverage / one contradiction / audit durability), not design-fatal.

## Consolidated fixes (spec-text, round 2)
1. **[F1-1 + O1-1 + G3-1a/1b — AC coverage of the anti-gaming falsifiers]** Add ACs (runnable proofs) pinning the load-bearing cases now only in R1/R2 "Falsified if": (a) sub-threshold-after-refutation STILL blocks on the floor (6 APPROVE + 2 refuted RC, threshold 7 → Block); (b) unrecognized/latest-round non-standard verdict BLOCKS and is NON-refutable (5 APPROVE + 1 unrecognized → Block; a refutation targeting it still → Block); (c) a refuted RC never increments Approves; (d) the no-advertise Block-message rule operationalized as a checkable predicate (block message names the unresolved slot(s), contains no paste-able `panel refute`/refutation incantation) — mirror HC-7's fixed-string style.
2. **[F3-1 — AC1 vs AC5 contradiction]** AC5's "existing tests pass without semantic modification" is jointly unsatisfiable with AC1: three pre-existing fixtures pin the exact RC-tolerated pass R1 removes — internal/panel/panel_decision_test.go:185-193 (5A+1 dissent→Allow), internal/complete/panel_advisory_test.go:68-82 (TestPanelAdvisory_Passing_WouldPass 2A+1RC→would PASS), cmd/mindspec/panel_test.go:386-389 (5A+1RC→exit 0). Add an explicit AC5 carve-out: those specific RC-tolerance fixtures WILL change semantically (that is R1's point + must be updated in-scope); AC5's no-semantic-modification applies to all OTHER tests. Also STRIKE the dead "if OQ3 resolves to a CLI verb" conditional in In Scope (OQ3 resolved to hand-edit).
3. **[G2-1 — durable refutation audit (design-strengthening)]** R2 currently says refutation is "always audited, never silent" but locates the panel_refuted write on the existing BEST-EFFORT post-mutation path (writePanelAuditMetadata swallows MergeMetadata errors as warnings; called only after completeErr==nil). For refutation — which CHANGES the gate outcome (unlike abandon, a terminal non-merge exit) — a swallowed audit failure = a silent, un-audited override that clears the gate = the "never silent" violation + gameable. AMEND R2: the refutation audit must be DURABLE — `mindspec complete` MUST NOT complete-successfully unless the `panel_refuted` metadata for the ACTUALLY-APPLIED slot+round refutation is written; plumb the validated/applied refutation(s) from PanelGateDecision/panelGate into writePanelAuditMetadata (audit only refutations that actually cleared a latest-round RC — never stale/invalid/unused); add an AC/test for metadata-write-failure-on-applied-refutation → must NOT silently complete without panel_refuted.
4. **[O2 minor]** ADR-0037 amendment text: enumerate §1 (schema addition for the `refutations` array) alongside §3/§7/§6/§8, for precedent parity (cf. 112 gate / 109 approve_threshold §1 additions).
5. **[O1 minor]** ADR/skill text crisp that a refutation's reason+evidence carry its legitimacy (empty-reason clears, parity with empty-abandon_reason ADR-0037 §8 footgun — acceptable but documented). Add a test-note for the pre-existing duplicate-slot-files-at-latest-round ambiguity.

Do NOT change the resolved OQs or the core R1/R2 model — these are AC/contradiction/audit-durability refinements. Keep all OQs [x]. Re-validate 0 ERRORs.
