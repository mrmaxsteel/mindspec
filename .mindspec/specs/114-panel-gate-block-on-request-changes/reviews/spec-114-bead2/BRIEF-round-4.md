# spec-114-bead2 — Round 4 Bead Panel (8 reviewers) — REDESIGN: discharge path DELETED

**Bead**: `mindspec-mvp8.2`. **Worktree**: `/Users/Max/replit/mindspec/.worktrees/worktree-spec-114-panel-gate-block-on-request-changes/.worktrees/worktree-mindspec-mvp8.2`
**Branch**: `bead/mindspec-mvp8.2` @ **33a8ce8dfa60b1c80660fd387dca3fcb46ef52ec** (prior rounds: R1 `ce9f5395`, R2 `7de2c459`, R3 `6f6634b4`).
**Panel**: 8 slots — O1–O3 Opus, S1–S3 Sonnet, F1 Fable, **G1 codex (gpt-5.6-sol)**. **Pass = 8/8 UNANIMOUS APPROVE.** Findings fixed or evidenced-refuted, never out-voted.

**READ `BRIEF.md`** (same dir) for the full Bead-2 mechanism (schema, gate validation, applied-refutation plumbing, anti-gaming). This round reviews a REDESIGN of ONE part: the durable-obligation reconciliation.

**READ-ONLY RULE (HARDENED)**: ALL build/test/mutation work in an ABSOLUTE `/tmp` copy (`git archive`), NEVER the live worktree. Pin reads to `33a8ce8d`. Verdict ONLY to the absolute path at bottom. Leave `git status` clean.

## Why the redesign (operator decision)
Rounds 1–3 each found a deeper FALSE-DISCHARGE hole (codex G1, 0.99 each): the reconciliation re-read MUTABLE panel files to "verify" a refuted finding had resolved naturally, and an operator could rearrange/delete/corrupt/alias panel files to fake that. The OPERATOR DECIDED (Max, 2026-07-10) to **DELETE the discharge-by-re-tally path entirely** rather than keep patching its identity precision. See `consolidated-round-3.md` (same dir) for the decision + spec.

## The new design (delta `git diff 6f6634b4 33a8ce8d`; net +374/−881 — a big SIMPLIFICATION)
The durable-obligation is now settled ONLY by the honest `panel_refuted` audit, written from a SELF-CONTAINED marker. There is no "resolved naturally" concept anymore.
1. **Self-contained marker**: `refutationPendingEntry` now carries `{slot, round, reason, evidence}` (the full refutation content, persisted at apply time from the panel.json refutations entry). The `Panels`/origin field is DROPPED.
2. **Reconciliation = flush pending → `panel_refuted` from the marker**: for every `refutation_pending` entry NOT already covered by a durable `panel_refuted` (already-covered no-op kept), write `panel_refuted{slot,round,reason,evidence,timestamp}` FROM THE MARKER. NO re-tally, NO panel read, NO origin matching. `reconcilePendingRefutations` is now `(beadID string) error` — no roots, no applied set, ZERO panel reads. Uniform on panel-present / no-panel / hatch paths + after panel.json removal, because the marker is self-contained.
3. **DELETED** (grep-clean repo-wide — verify): `dischargeVerified`, `dischargeEvidence`, `dischargedEntry`, `decodeDischargedEntries`, `unionDischargedEntries`, `writeRefutationDischargedMetadata`, `dedupeSortedSlugs`, the `refutation_discharged`/`_at`/`_entries` keys, `appliedOrigins` + all origin logic in `panelGate`, and the reconcile-time `panel.ForBead`/`panelTallyFn` re-tally.
4. **`panelGate` signature changed** back to `(*panel.Registration, error)` (dropped the `[]panel.Refutation` return — the applied set is now internal to the marker write); all call sites updated.
5. **Guard added**: a shape-invalid marker entry (empty slot / round < 1) ⟹ Refuse (fail-closed).
6. **Natural resolution (AC4b)** by construction: a re-panel flip with NO refutation applied writes NO marker → NO obligation → zero-ceremony completion. Needs no discharge.

## What to verify (round 4 — review the NEW mechanism as a whole)
1. **The whole false-discharge class is GONE (G1 codex — the crux, + O1/F1)** — there is no re-tally, no discharge, no panel read at reconcile, so the rounds-1/2/3 attacks (wrong-panel, origin-removed, origin-corrupted, basename-alias) are structurally impossible. CONFIRM: `grep -rn 'refutation_discharged|dischargeEvidence|dischargeVerified|ForBead' ` in reconcile is empty; `reconcilePendingRefutations` reads NO panel directory. Re-run your prior repros — each should now SATISFY from the marker (write the honest `panel_refuted`) and complete, or be structurally impossible. Try to find ANY new way an obligation merges un-audited.
2. **Self-contained marker is correct (O1/G1/S1)** — the marker persists slot/round/reason/evidence at apply time (from the panel.json refutations entry); `panel_refuted` is written FROM the marker (not re-read from panel.json), so it lands even when panel.json is gone/changed. `TestPanelRefuted_SatisfyFromMarker_PanelGone` proves it. Union keeps (slot,round)-dedup, back-fills empty reason/evidence, never drops.
3. **Kept guards intact (O1/O2/F1/S2)** — applied≡durably-persisted (marker write part of the allow-decision; union not replace; read OR write failure ⟹ Block); reconcile-before-close on EVERY path incl. hatch (a pre-existing pending satisfied from marker before close even under `MINDSPEC_SKIP_PANEL`/`enforcement.panel_gate:false`); already-covered no-op ((slot,round)-exact); fail-closed `MergeMetadata`+`GetMetadata`; malformed marker/entries ⟹ Refuse; shape-invalid entry ⟹ Refuse. Mutation-probe: swallowing the reconcile `panel_refuted` write error must turn a named test RED (the fixer reports 4 do).
4. **Anti-gaming byte-unchanged (O2)** — `git diff ce9f5395 33a8ce8d -- internal/panel/` is EMPTY (tally.go/gate.go untouched) → AC3/7/8/9/12 all preserved. Confirm + run `go test ./internal/panel -run 'Refut|Round|Dup|PanelGateDecision'`.
5. **Redesign integrity — nothing lost in the edit (S1/S3 + G1 empirical)** — the fixer reported a scare (a `git checkout --` briefly wiped uncommitted edits, then re-applied + re-verified before commit). CONFIRM the committed state is COMPLETE and self-consistent: `panelGate` 2-tuple arity is consistent across ALL call sites (`grep -rn 'panelGate('`), no orphaned references to deleted symbols, no half-applied edit. Build + full test + `-race` green in /tmp.
6. **`panelGate` signature change (O3/S3)** — 3-tuple→2-tuple; all call sites (1 prod + the test sites) updated mechanically, outcome-preserving. Scope = internal/panel|complete|bead only, one commit, no ADR/plan/doc edits (plan + ADR updates are handled separately by the orchestrator/Bead 3).
7. **Tests (S2 + all)** — removed the 5 discharge tests + the inverted `CrossRun_PanelRemoved_Refuses` (→ `SatisfyFromMarker_PanelGone`); added `SatisfyFromMarker_PanelGone`, `NaturalResolution_NoObligation`, `MarkerCarriesReasonEvidence`; rewired union/hatch/already-covered/malformed to marker semantics. Are the new/rewired tests REAL (mutation-probe the load-bearing ones)? Any coverage lost with the deleted tests that still matters?

## Empirical (each slot, in /tmp)
`go build ./...`; `gofmt -l ./cmd ./internal` (empty); `go vet ./internal/panel ./internal/complete ./internal/bead`; `go test ./internal/panel -run 'Refut|Round|Dup|PanelGateDecision'`; `go test ./internal/complete -run 'PanelRefuted|PanelGate'`; `go test ./internal/bead -run 'MergeMetadata|GetMetadata'`; `go test ./internal/panel ./internal/complete ./internal/bead ./cmd/mindspec`; `go test -race` on the three. (z4ps `TestRun_IdleNoBeads` in internal/instruct + internal/harness LLM tests are pre-existing/environmental — ignore.) Report actual output.

## Per-slot lens
- **G1 codex** — the false-discharge class is structurally gone (re-run your 3 prior repros → now satisfy-from-marker or impossible); hunt for ANY new un-audited-merge path in the simpler design. **O1** self-contained-marker correctness + applied≡persisted + reconcile-before-close every path. **O2** anti-gaming byte-unchanged + kept-guard soundness. **O3** scope/signature-change/no-regression. **S1** codebase-pin + redesign-integrity (nothing lost in the checkout scare). **S2** tests real (mutation-probe; lost-coverage check). **S3** scope fence + arity consistency. **F1** adversarial (find any un-audited merge, any lost guard, any hollow new test).

Verdict: APPROVE / REQUEST_CHANGES / REJECT. Output JSON to `<this-dir>/<your-slot>-round-4.json` — keys `reviewer_id`, `verdict`, `confidence`, `rationale` (≤200 words), `concrete_changes_required`, `findings` (mark prior findings ADDRESSED/OBVIATED).
