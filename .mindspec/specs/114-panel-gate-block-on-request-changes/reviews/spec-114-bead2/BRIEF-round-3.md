# spec-114-bead2 — Round 3 Bead Panel (8 reviewers) — origin-binding fix re-verify

**Bead**: `mindspec-mvp8.2`. **Worktree**: `/Users/Max/replit/mindspec/.worktrees/worktree-spec-114-panel-gate-block-on-request-changes/.worktrees/worktree-mindspec-mvp8.2`
**Branch**: `bead/mindspec-mvp8.2` @ **6f6634b45c931dba4c0e3f33af32a6b89f4b0c81** (round-2 tip was `7de2c459`; round-1 `ce9f5395`).
**Panel**: same 8 slots — O1–O3 Opus, S1–S3 Sonnet, F1 Fable, **G1 codex (gpt-5.6-sol)**. **Pass = 8/8 UNANIMOUS APPROVE.** Findings fixed or evidenced-refuted, never out-voted.

**READ `BRIEF.md` + `BRIEF-round-2.md`** (same dir) for the full mechanism + round-2 context. This round re-verifies ONE fix: origin-binding.

**READ-ONLY RULE (HARDENED)**: ALL build/test/mutation work in an ABSOLUTE `/tmp` copy (`git archive`), NEVER the live worktree. Pin reads to `6f6634b4`. Verdict ONLY to the absolute path at bottom. Leave `git status` clean.

## The finding this round closes (round-2 blocking, found by BOTH G1 codex 0.99 AND F1 fable 0.72, each E2E-reproduced)
Round-2's fix re-scanned all *currently-surviving* matched panels but the pending obligation stored only slot+round, NOT its origin. So an operator could settle a real refutation as a no-questions "resolved naturally" discharge by making the ORIGIN panel disappear — DELETE `zzz-origin/panel.json`, OR CORRUPT it (`panel.ForBead` drops `Err != nil` regs, same as deletion) — while a DECOY resolved panel (`aaa-first/X=APPROVE`) remained: discharge fired, false `refutation_discharged X@1` written, bead closed+merged though the origin RC never resolved.

## The round-3 fix (delta `git diff 7de2c459 6f6634b4` — 2 files, `internal/complete/` only)
1. **Origin recorded at persist**: `refutationPendingEntry` gained `Panels []string json:"panels"` (origin panel slugs = `review/<slug>` basenames, sorted+deduped). `panelGate`'s per-registration loop accumulates `appliedOrigins[key] = append(..., regs[i].Slug())` for each `d.AppliedRefutations` entry (all origins if a slot+round cleared in multiple panels), passed to `persistRefutationPending(beadID, applied, origins)`. panelGate's public signature is UNCHANGED (no call-site ripple). Cross-run unions of the same key MERGE origin sets (strictly more conservative).
2. **Discharge bound to origin**: `dischargeVerified` now requires — `retallyOK` AND `len(p.Panels) > 0` (fail-closed on missing origin) AND EVERY origin slug present in the current `panel.ForBead` matched set (presence ⟹ readable; `ForBead` drops `Err != nil`, catching corruption) AND `dischargeEvidence` in EVERY origin's re-tally AND (round-1 guard preserved) `dischargeEvidence` in every OTHER matched panel too. Any origin absent/unreadable/still-live-RC → NOT discharge → Satisfy-if-applied-this-run, else REFUSE ("restore the origin panel(s)…").
3. Fixture updates (mechanical): seeded pending entries whose tests intend discharge now carry their origin slug (`seedPending` gained `originSlug`; CrossPanel→`zzz-origin`; NaturalResolution twin; WarnPath; corruptAuditCase). Covered/Refuse-only seeds left origin-less deliberately.
4. Dead `res == nil` guard resolved via a defensive comment.
New tests: `TestPanelRefuted_OriginRemoved_Refuses`, `TestPanelRefuted_OriginCorrupted_Refuses` (+ positive controls).

## What to verify (round 3)
1. **The origin-removal + origin-corruption holes are CLOSED (G1 codex + F1 — the crux)** — re-run the round-2 E2E repros in /tmp: (a) delete `zzz-origin/panel.json`, keep decoy `aaa-first/X=APPROVE`, retry under `MINDSPEC_SKIP_PANEL` → MUST Refuse, NO `refutation_discharged`, NO close/merge; (b) same but CORRUPT `zzz-origin/panel.json` instead → MUST Refuse; (c) origin disappears BETWEEN gate scan and reconcile scan → MUST Refuse. Confirm a decoy non-origin resolved panel can NEVER substitute for a missing/corrupt origin. Mutation-proof: revert origin-binding (discharge vs currently-matched) → `TestPanelRefuted_OriginRemoved_Refuses` RED. Any residual origin-unbound discharge path = REQUEST_CHANGES.
2. **Origin recording is correct + complete (O1/G1)** — `appliedOrigins` captures the RIGHT panel(s): exactly the matched panels whose tally had that slot+round as a latest-round RC the refutation cleared. Multiple origins recorded when applicable; cross-run union merges (never drops) origins. A malformed/missing `panels` field → Refuse (fail-closed).
3. **Legitimate natural resolution STILL works (O1/O2/S2)** — AC4b zero-ceremony re-panel: origin panel persists and its re-tally advances (LatestRound>round) or the slot flips APPROVE → discharge fires against the actual origin (not blocked). Verify the existing natural-resolution + discharge tests still pass. Single-panel completion unaffected.
4. **No regression to the ~27 confirmed guards (all)** — every round-1 + round-2 `TestPanelRefuted_*`/`TestPanelGate_*` still passes (marker-write-fail Block, union, hatch-reconciles, fail-closed MergeMetadata/decode, AC7/8/9/12, reconcile-before-close, already-covered no-op, malformed→Refuse, both-panels-present cross-panel Refuse). The round-2 `CrossPanel_NoFalseDischarge`/`AlreadyCoveredPending_Completes`/`MalformedPendingEntries_Refuses` all still green.
5. **Anti-gaming: does origin-binding open any NEW hole? (O2/F1)** — could an operator ADD a fake origin, or exploit the union-merge, or use the `len(Panels)>0` requirement to game a discharge or a Satisfy? Trace it. The origin requirement should make discharge strictly HARDER.
6. **Scope + empirical (S1/S3 + G1)** — delta = 2 files, `internal/complete/` only, one commit, no panel/bead/ADR/doc edits, panelGate arity unchanged. In /tmp: `go build`; `gofmt -l ./cmd ./internal` (empty); `go vet`; the plan verification commands; full sweep; `-race` on panel/complete/bead. z4ps `TestRun_IdleNoBeads` pre-existing. Report actual output.

## Per-slot lens
- **G1 codex** — re-verify BOTH your round-1 AND round-2 blocking findings are now ADDRESSED (run the removal + corruption + between-scans repros). **O1** origin recording correctness + reconcile-before-close ordering + natural-resolution preserved. **O2** anti-gaming (does origin-binding open a new hole?) + strictness. **O3** scope/provenance/no-regression. **S1** codebase-pin. **S2** the 2 new tests real (mutation-probe) + natural-resolution intact. **S3** scope fence. **F1** adversarial re-probe (find ANY residual origin-unbound discharge, or a new hole from the origin field).

Verdict: APPROVE / REQUEST_CHANGES / REJECT. Output JSON to `<this-dir>/<your-slot>-round-3.json` — keys `reviewer_id`, `verdict`, `confidence`, `rationale` (≤200 words), `concrete_changes_required`, `findings` (mark prior findings ADDRESSED/MISSED).
