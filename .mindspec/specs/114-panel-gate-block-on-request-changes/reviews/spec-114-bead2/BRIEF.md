# spec-114-bead2 — Round 1 Bead Panel (8 reviewers) — R2 durable-refutation escape + audit

**Bead**: `mindspec-mvp8.2` (spec 114, Bead 2 = R2 — the subtlest bead). **Worktree**: `/Users/Max/replit/mindspec/.worktrees/worktree-spec-114-panel-gate-block-on-request-changes/.worktrees/worktree-mindspec-mvp8.2`
**Branch**: `bead/mindspec-mvp8.2` @ **ce9f5395ebf93444989aa724c51e501fdeb7d041** (parent `1906e39e` = spec tip with Bead 1's leg 9.5).
**Panel**: 8 slots — O1–O3 Opus, S1–S3 Sonnet, F1 Fable, **G1 codex (real gpt-5.6-sol)**. **Pass = 8/8 UNANIMOUS APPROVE.** Every finding adjudicated (fixed or evidenced-refuted), NEVER out-voted.

**READ-ONLY RULE (MANDATORY, HARDENED — this session already had a /tmp-fence violation that corrupted a shared worktree)**: Do ALL build/test/mutation work in an ABSOLUTE `/tmp` copy (`git archive`/`rsync`), NEVER in the live worktree — not even transiently. Pin reads to `ce9f5395`. Write your verdict ONLY to the absolute path at the bottom. Leave `git status` clean. Codex: your workdir sandbox includes /tmp — do your build/test in an absolute /tmp clone, write the verdict to the absolute review path.

## What Bead 2 does (build on Bead 1's leg 9.5)
Bead 1 made an unresolved REQUEST_CHANGES block `mindspec complete` (leg 9.5). Bead 2 adds the ONLY per-slot escape: an **audited refutation**. The operator hand-edits `panel.json` adding a `refutations: [{slot,round,reason,evidence}]` entry; the GATE (not the record surface) validates it and enforces a DURABLE audit obligation. The plan is `.mindspec/specs/114-panel-gate-block-on-request-changes/plan.md` **lines 377–874** (Bead 2, steps 1–7) — READ IT; it is the contract you verify against. Core mechanism:
1. **Schema** (`internal/panel/panel.go`) — `Refutation{Slot,Round,Reason,Evidence}` + `Refutations []Refutation` on `Panel`, parse-lenient exactly like `AbandonReason` (missing/empty fields are consumer concerns, never a parse error → never tips fail-OPEN; a TYPE-mismatch fails the whole unmarshal → leg-2 Block = fail-CLOSED, the safe direction).
2. **Single-homed validation** (`internal/panel/tally.go`) — `UnresolvedVerdicts()` treats a latest-round RC as RESOLVED iff a `Refutations` entry has byte-equal `Slot` AND `Round == LatestRound`; `AppliedRefutations()` returns exactly the matched entries, DEDUP by (slot,round), slot-sorted. `Approves` is NEVER touched (AC9). REJECT/hard_block/unrecognized are never refutable (excluded at source; leg 9 fires first).
3. **Plumb out** (`internal/panel/gate.go`) — `Decision.AppliedRefutations` populated ONLY on the leg-10 Allow return; `VoteDecision` lockstep; the one `Decision` struct-comparison fixed to field-wise (Decision stops being Go-comparable).
4. **panelGate** (`internal/complete/panel_advisory.go`) — signature → `(*panel.Registration, []panel.Refutation, error)`; dedup across matched panels; 1 prod + 12 test call sites updated.
5. **DURABLE-OBLIGATION protocol** (`complete.go` + `panel_advisory.go`) — (a) "applied" ≡ durably-persisted: a `refutation_pending` marker write folded INTO panelGate's Allow-with-refutations path, read-then-UNION-then-write (never bare replace — `MergeMetadata` replaces at top-level key granularity, so a naive write clobbers an older unsatisfied pending), fail-closed → read OR write failure returns a **Block** (not abort-with-applied); (b) `writePanelRefutedMetadata` non-swallowing, UNIONs `panel_refuted_entries`; (c) `reconcilePendingRefutations` in `Run` BEFORE the step-4 close on EVERY path (panel-present, no-panel, hatch) — re-tallies `panelReg.Dir` via `panel.Tally` for discharge vote-state; each pending entry → **Satisfy** (this run's applied covers it → `panel_refuted`) / **Discharge** (ONLY on verified re-tally: `LatestRound` > pending round, OR at `==` round the slot's latest verdict present-and-not-RC → `refutation_discharged`) / **Refuse** (recoverable `guard.NewFailure`); (d) `completeGetMetadataFn` read-error ⟹ REFUSE (fail-closed); (e) hatch paths reconcile too (except the GATE, not the obligation).
6. **Fail-closed metadata** (`internal/bead/bdcli.go`) — `MergeMetadata` RETURNS the error on a read/parse failure instead of proceeding from empty (closes the post-write erasure hole); new exported `GetMetadata`. A clean-but-absent read still merges from empty.

## What to verify (each concern → a disposition; the subtle parts are the whole point)
1. **Durable-audit is genuinely un-loseable (G1 codex + O1 — LOAD-BEARING; this is where gpt-5.6-sol found real holes at plan stage).** The net invariant (plan L612-628): a refutation that EVER durably clears an RC can never let its bead close/merge — across ANY retry, ANY path (panel-present/no-panel/hatch), even after `panel.json` removal — without a durable `panel_refuted` (satisfied) or VERIFIED `refutation_discharged` (tally-proven moot) audit. Trace the code: is (a) applied≡persisted really enforced (marker-write/union-read failure ⟹ Block, NOT a silent clear)? Is the reconciliation reached BEFORE close on every path? Is discharge keyed on the RE-TALLY of `panelReg.Dir` (NOT a bare gate-pass — a Warn abandoned/missing-ref/transient-gitErr must NOT falsely discharge)? Is the UNION real (a newer marker never clobbers an older unsatisfied pending)? Find ANY path where the audit is lost while the merge lands = REQUEST_CHANGES.
2. **Fail-closed `MergeMetadata`/`GetMetadata` (G1/O1/S1)** — `internal/bead/bdcli.go`: a read/parse error now RETURNS (no replace-write from empty, which would erase `panel_refuted`); a clean absent-metadata read still merges from empty (no false-positive fail). `GetMetadata` surfaces read errors. Blast-radius grep (`grep -rn 'MergeMetadata(' --include='*.go'`) — confirm no caller class regresses (already-fatal repair.go/phase.EnsureMigrated; warn complete+approve; ignore release.go).
3. **Anti-gaming layer holds (O2/G1)** — a refutation clears ONLY canonical latest-round RC: never REJECT/hard_block (AC3), never unrecognized (AC8 non-refutable), never a newer-round re-RC (AC4b), never another slot; one entry clears exactly one slot+round. `Approves` never incremented by a refuted RC (AC9) → sub-threshold-after-all-RCs-refuted STILL blocks on the leg-10 floor (AC7). AC12: byte-exact slot matching (slot "x" never clears a REJECT under "X"); two entries same slot+round collapse to ONE applied record.
4. **AC10 no-advertise (O2/S2)** — the leg-9.5 unresolved-RC Block message still carries no `refut` substring even when a `refutations` array is present. (The marker-write-failure Block message IS allowed to say "refutation" — it reports an attempted-but-unrecorded refutation, explicitly outside AC10 per plan L510-516; confirm that distinction is honored.)
5. **Tests real, not hollow (S2/S3/F1 + G1 empirical)** — the 7(A) decision tests (`TestPanelGateDecision_Refutations`, `TestVoteDecision_Refutations`, `TestResult_AppliedRefutations`) and the 7(B) durable-obligation e2e suite (`TestPanelRefuted_*` (i)-(xiii) + `internal/bead` `TestMergeMetadata_FailClosedOnReadError`/`TestGetMetadata`). Mutation-probe the load-bearing ones in /tmp: disable the marker-write-fail Block, or the fail-closed read, or the discharge re-tally check — do the corresponding tests go RED? Assess the 3 documented deviations (Warn-discharge test drives reconcile directly; erasure-survival models the contract cross-package; full-repo test hung on unrelated internal/harness) — are they legitimate or do they leave a real claim unpinned?
6. **Scope + provenance (S3/O3)** — exactly the 14 files (internal/panel|complete|bead), one commit. Every AC2/3/4/7/8/9/10/11/12 mapped to a named passing test (plan Provenance L1177-1193). The `Decision`-comparability fix + the 12 mechanical `panelGate` caller updates are outcome-preserving (no fixture flips beyond Bead-1's four). No ADR edits (that's Bead 3).
7. **Empirical (G1 codex + verify each slot)** — in a /tmp clone: `go build ./...`; `gofmt -l ./cmd ./internal` (empty); `go vet ./internal/panel ./internal/complete ./internal/bead`; the plan's verification commands (`go test ./internal/panel -run 'Refut|Round|Dup|PanelGateDecision'`, `go test ./internal/complete -run 'PanelRefuted|PanelGate'`, `go test ./internal/bead -run 'MergeMetadata|GetMetadata'`), then `go test ./internal/panel ./internal/complete ./internal/bead ./cmd/mindspec` full (z4ps `TestRun_IdleNoBeads` in internal/instruct is pre-existing — ignore/`-skip`). `go test -race` on panel/complete/bead. Report actual output.

## Per-slot lens defaults
- **G1 codex (gpt-5.6-sol)** — durable-audit feasibility + fail-closed metadata + empirical (RUN the tests + mutation-probe the un-loseable-audit claim; you found the plan-stage holes — hunt the impl for the same). **O1 Opus** — durable-obligation ordering + applied≡persisted + reconcile-every-path (LOAD-BEARING). **O2 Opus** — anti-gaming (refute-only-RC, AC7/AC9/AC12 layered floor) + AC10 no-advertise. **O3 Opus** — decomposition/provenance (every AC→named test) + scope + no-regression.
- **S1 Sonnet** — codebase-pin (symbols/tests exist + green; `MergeMetadata`/`GetMetadata` seam). **S2 Sonnet** — tests real (7A/7B not hollow; the 3 deviations). **S3 Sonnet** — scope fence + AC5 fixture classification (the Decision-comparability + panelGate-arity updates outcome-preserving).
- **F1 Fable** — adversarial (mutation-probe: can a durable audit be lost while the merge lands? can a Warn path falsely discharge? can a marker clobber an older pending? is any e2e test hollow?).

Verdict: APPROVE / REQUEST_CHANGES / REJECT. Output JSON to `<this-dir>/<your-slot>-round-1.json` with keys: `reviewer_id` ("<slot> <family>"), `verdict`, `confidence` (0–1), `rationale` (≤200 words), `concrete_changes_required` (empty if APPROVE), `findings`.
