# spec-114-bead2 — Round 2 Bead Panel (8 reviewers) — fix-up re-verify

**Bead**: `mindspec-mvp8.2` (spec 114, Bead 2). **Worktree**: `/Users/Max/replit/mindspec/.worktrees/worktree-spec-114-panel-gate-block-on-request-changes/.worktrees/worktree-mindspec-mvp8.2`
**Branch**: `bead/mindspec-mvp8.2` @ **7de2c459b06a68320868a533687b5851d59507a8** (round-1 tip was `ce9f5395`).
**Panel**: same 8 slots — O1–O3 Opus, S1–S3 Sonnet, F1 Fable, **G1 codex (gpt-5.6-sol)**. **Pass = 8/8 UNANIMOUS APPROVE.** Every finding fixed or evidenced-refuted, never out-voted.

**READ the round-1 BRIEF** (`BRIEF.md`, same dir) for the full Bead-2 mechanism. This round is a FOCUSED re-verify of the round-1 fix-up — but you still own your round-1 lens for regression.

**READ-ONLY RULE (MANDATORY, HARDENED)**: ALL build/test/mutation work in an ABSOLUTE `/tmp` copy (`git archive`), NEVER the live worktree — not even transiently. Pin reads to `7de2c459`. Verdict ONLY to the absolute path at the bottom. Leave `git status` clean.

## What changed since round 1 (the entire round-2 delta — 3 files, all `internal/complete/`)
Round 1 was 7 APPROVE / 1 REQUEST_CHANGES. The RC was **G1 codex (0.99, blocking): cross-panel false discharge** — reconciliation re-tallied only the FIRST matched panel while applied refutations aggregate across ALL, so a pending could be discharged from the wrong panel's APPROVE state while its origin RC stayed live (G1 reproduced under `MINDSPEC_SKIP_PANEL` with two panel dirs, contradictory same-slot verdicts). Two more findings (major + minor) were folded in. The fix commit `7de2c459` (diff `git diff ce9f5395 7de2c459` — `complete.go`, `panel_advisory.go`, `panel_refuted_test.go`):
1. **[BLOCKING fix] All-panel discharge verification** — `reconcilePendingRefutations` now takes `roots []string` (not a single `panelReg`), re-scans via `panel.ForBead(panelScanFn(roots...), beadID)` for EVERY matched registration, and re-tallies EACH. A pending (slot,round) DISCHARGES only when `dischargeEvidence` holds in **every** matched panel; any panel still showing the RC live, any erroring re-tally, or zero matched panels → veto → Refuse (then Satisfy-if-applied-this-run, else Refuse). `complete.Run` computes `gateRoots` once and passes it to BOTH `panelGate` and `reconcilePendingRefutations` through the identical `panelScanFn` seam (so the two can't diverge on the matched set). panelGate's 3-tuple return + 13 call sites are UNCHANGED (re-scan chosen over arity change). New test `TestPanelRefuted_CrossPanel_NoFalseDischarge`.
2. **[MAJOR fix] Already-covered pending (plan L534)** — before satisfy/discharge/refuse, a pending exactly (slot,round)-matched by a durable `panel_refuted_entries` OR `refutation_discharged_entries` (same fail-closed read) reconciles to a no-op. New test `TestPanelRefuted_AlreadyCoveredPending_Completes`.
3. **[MINOR fix] Fail-closed decode** — the three decode helpers now return `(entries, error)`: absent key → `(nil,nil)` no-op; present-but-malformed → error → Refuse (persist path → panelGate Block; audit writers → fail pre-close). New test `TestPanelRefuted_MalformedPendingEntries_Refuses`.

**Deliberate strictness (assess it):** `dischargeEvidence` must hold in EVERY matched panel, not just panels that contain the slot — because a pending isn't bound to its origin panel, a same-round matched panel *lacking* the slot is indistinguishable from the origin with its verdicts removed, so the fixer treats a slot-absent matched panel as non-confirming (Refuse). Over-conservative, recoverable. Is this sound, or does it wrongly block a legitimate multi-panel completion?

## What to verify (round 2)
1. **G1's blocking finding is truly fixed (G1 codex — the crux + O1)** — re-run your round-1 /tmp repro (two panel dirs, `aaa-first/X=APPROVE` + `zzz-origin/X=RC`, under `MINDSPEC_SKIP_PANEL`): reconciliation must now REFUSE (no false discharge), and only discharge once the origin panel's slot actually resolves. Confirm the all-panel re-scan is correct: does `panel.ForBead(panelScanFn(gateRoots...), beadID)` surface the SAME matched set the gate used? Can gate & reconcile still diverge on the matched set? Any residual false-discharge path = REQUEST_CHANGES.
2. **The deliberate every-matched-panel strictness (O1/O2/F1)** — is "discharge only if evidence holds in EVERY matched panel (slot-absent panel = non-confirming → Refuse)" sound and safe? It's more conservative than the consolidated doc's parenthetical; confirm it never LOSES an audit and its only cost is a recoverable Refuse. Does it wrongly block a normal single-panel completion? (It must not — a single matched panel that resolves the slot still discharges.)
3. **Fixes #2 and #3 sound (S2/G1)** — already-covered pending → no-op (both coverage kinds, panel.json absent); the (slot,round)-exactness Refuse control; malformed-entries → Refuse (fail-closed, symmetric with rule (d)); absent key → no-op. Mutation-probe: revert each fix in /tmp → does its new test go RED?
4. **No regression to the ~25 round-1 guards (all)** — every pre-existing `TestPanelRefuted_*`/`TestPanelGate_*` still passes (only `WarnPathDoesNotDischarge`'s signature updated mechanically, same Refuse assertion). The marker-write-fail Block, discharge-keys-on-retally, union-not-replace, hatch-reconciles, fail-closed MergeMetadata, AC7/8/9/12, reconcile-before-close ordering all still hold.
5. **Scope + empirical (S3/S1 + G1)** — delta is exactly 3 files (`internal/complete/`), one commit, NO panel/bead/ADR/doc edits. In a /tmp clone: `go build ./...`; `gofmt -l ./cmd ./internal` (empty); `go vet`; the plan verification commands; `go test ./internal/panel ./internal/complete ./internal/bead ./cmd/mindspec`; `go test -race` on the three. z4ps `TestRun_IdleNoBeads` pre-existing — ignore. Report actual output.

## Per-slot lens (same as round 1, applied to the delta + regression)
- **G1 codex** — re-verify YOUR blocking finding is fixed (re-run the multi-panel repro; confirm all-panel discharge). **O1** durable-obligation ordering + the re-scan matched-set consistency. **O2** anti-gaming unchanged + the strictness soundness. **O3** scope/provenance/no-regression. **S1** codebase-pin (new symbols/tests green). **S2** tests-real (mutation-probe the 3 fixes). **S3** scope fence. **F1** adversarial re-probe (can any false-discharge / lost-audit / hollow test survive the fix?).

Verdict: APPROVE / REQUEST_CHANGES / REJECT. Output JSON to `<this-dir>/<your-slot>-round-2.json` — keys `reviewer_id`, `verdict`, `confidence`, `rationale` (≤200 words), `concrete_changes_required`, `findings`.
