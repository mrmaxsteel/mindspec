# spec-114-bead1 — Round 1 Bead Panel (8 reviewers) — R1 gate-block

**Bead**: `mindspec-mvp8.1` (spec 114, Bead 1 = R1). **Worktree**: `/Users/Max/replit/mindspec/.worktrees/worktree-spec-114-panel-gate-block-on-request-changes/.worktrees/worktree-mindspec-mvp8.1`
**Branch**: `bead/mindspec-mvp8.1` @ **4dfebbcba61bd571d15b8c4788752409504d017d** — `feat(panel): R1 gate — unresolved REQUEST_CHANGES / unrecognized verdict blocks (leg 9.5, layered) + VoteDecision twin + AC5 fixtures`
**Panel**: 8 slots — O1–O3 Opus, S1–S3 Sonnet, F1 Fable, **R8 sonnet-sub** (Claude standing in for the codex slot; no codex on bead panels this session). **Pass = every finding adjudicated (fixed or evidenced-refuted), never out-voted.**

**READ-ONLY RULE (MANDATORY)**: verdict JSON only; pin reads to `4dfebbcb`; scratch under ABSOLUTE /tmp only (or `t.TempDir()`); leave `git status` clean. Verdict ONLY to the exact absolute path at the bottom — do NOT create a `reviews/` dir in the bead worktree.

## What the bead does (R1 — the RC-blocks gate change; refutation is Bead 2, NOT here)
Spec 114 makes `mindspec complete`'s gate BLOCK on any unresolved REQUEST_CHANGES (not just REJECT). Bead 1 ships R1: a new **leg 9.5** in `internal/panel/gate.go` `PanelGateDecision` — after leg 9 (REJECT/hard_block), before leg 10 (threshold Allow) — that Blocks when `f.Res.UnresolvedVerdicts()` (new `Result` method: latest-round verdicts that are neither `VerdictApprove` nor `VerdictReject`) is non-empty. LAYERED: leg 10's `approve_threshold`/N−1 floor stays a NECESSARY condition; leg 9.5 adds the RC/unrecognized-verdict block on top. In Bead 1 there is NO refutation (Bead 2 adds it) — so ANY unresolved RC blocks. The `VoteDecision` twin (`tally.go`) gets the identical check so complete's advisory + panel verify/tally + instruct never disagree on the vote portion. The Block message is a byte-SUPERSET of leg-10's (so sub-threshold fixtures keep their substring assertions), names the unresolved slot(s), ends with an ADR-0035 `recovery:` line (fix-and-re-panel), and prints NO paste-able refutation incantation (AC10).

## Files in scope (final state at 4dfebbcb) — exactly 9, NO internal/bead
`internal/panel/gate.go` (leg 9.5), `internal/panel/tally.go` (UnresolvedVerdicts + VoteDecision twin), and 7 test files: `panel_decision_test.go`, `panel_test.go`, `votedecision_test.go`, `panel_advisory_test.go`, `panel_gate_e2e_test.go`, `panelstate_test.go`, `cmd/mindspec/panel_test.go`.

## What to verify (each concern → a disposition)
1. **Leg 9.5 correctness (O1/O2)** — placed AFTER leg 9 (REJECT/hard_block), BEFORE leg 10 (threshold). LAYERED (threshold floor still necessary; 9.5 adds the RC-block). Fires on `UnresolvedVerdicts()` non-empty (latest-round verdict neither APPROVE nor REJECT — covers both unresolved RC AND unrecognized verdict strings). All OTHER legs (abandoned/round-mismatch/missing-ref/transient/stale/dirty/incomplete/REJECT/hard_block/threshold) BYTE-PRESERVED. Read the diff.
2. **VoteDecision lockstep (O2/S2)** — the identical check is in `VoteDecision` between its REJECT/hard_block + threshold legs → the vote-only twin can NEVER disagree with `PanelGateDecision` on the vote portion. Verify no divergence.
3. **AC5 fixture reconciliation (S3/F1/R8 — the sharp check)** — the plan named FOUR intended flips: (a) panel_decision_test.go "5A+1 dissent"→Block; (b) panel_advisory_test.go `TestPanelAdvisory_Passing_WouldPass`→renamed `_Dissent_WouldBlock` + new all-APPROVE `_Passing_WouldPass`; (c) cmd panel_test.go at-threshold→wantErr + all-APPROVE companion; (d) votedecision_test.go "5/6"→VoteBlock + all-APPROVE companion. AND 8 outcome-PRESERVING updates (RC/empty-verdict fillers → genuine APPROVEs; the panelstate `beadPanel` empty→real verdicts because empty is itself "unresolved" now). VERIFY: exactly these flip; NO fixture OUTSIDE the named set flips outcome (the diff-review proof). Any unclassified flip, or a preserving update that actually flips, is a finding.
4. **Message byte-superset (O2/R8)** — leg-9.5's message is a substring superset of leg-10's, so pre-existing sub-threshold fixtures routed through 9.5 keep their assertions green. Verify the superset holds (the sub-threshold Block fixtures pass).
5. **Tests real (O1/S1/S2)** — `TestPanelGateDecision_UnresolvedRequestChangesBlocks` (AC1: 5A+1RC→Block naming slot; AC8: unrecognized→Block; two-RC naming both; AC10 predicate), `TestPanelGate_RequestChangesBlocksComplete` (AC1 e2e: nothing mutated, recovery line), `TestPanelGateDecision_ApprovesExcludesUnresolvedVerdicts` (AC9: RC never increments Approves — asserted on the real Tally), `TestPanelGateDecision_ThresholdFloorIsLayeredNotReplaced` (AC7 precursor). (Full AC7/AC9 refutation-based falsifiers are Bead 2's per the plan — confirm Bead 1 does the precursor, not the refutation.) Are they real, not hollow?
6. **Scope (S3)** — exactly the 9 files; NO internal/bead, NO refutation code (that's Bead 2); one commit; the Decision struct's AppliedRefutations slice is NOT added here (Bead 2's).
7. **Empirical (R8 sonnet-sub)** — `cd` the worktree; `go build ./...`; `gofmt -l ./cmd ./internal` (empty); `go vet ./internal/panel ./internal/complete`; `go test ./internal/panel ./internal/complete ./internal/instruct ./cmd/mindspec` (green EXCEPT `internal/instruct` `TestRun_IdleNoBeads` = known pre-existing z4ps flake — confirm it fails identically on the base via git stash, do NOT fail the bead on it). Try to break it: can a non-APPROVE latest-round verdict slip past leg 9.5? does a clean all-APPROVE panel still pass (no false block)? does the byte-superset message actually keep the sub-threshold fixtures green?

## Per-slot lens defaults
- **O1 Opus** — leg-9.5 correctness + layering + author-of-record (diff ↔ plan Bead 1). **O2 Opus** — VoteDecision lockstep + message-superset + all-legs-byte-preserved. **O3 Opus** — contract/no-regression (existing gate behavior unchanged except the 4 intended flips).
- **S1 Sonnet** — codebase-pin (symbols/tests exist + green). **S2 Sonnet** — tests real (AC1/8/9 exercise the real paths, not hollow). **S3 Sonnet** — scope fence + AC5 fixture classification (4 flips + 8 preserving, no other flip).
- **F1 Fable** — adversarial (mutation-probe: can a non-APPROVE slip leg 9.5? is any test hollow? did an unclassified fixture flip?).
- **R8 sonnet-sub** — empirical (build/gofmt/vet/test; break-it; verify the z4ps flake is pre-existing).

Verdict: APPROVE / REQUEST_CHANGES / REJECT. Output JSON to `<this-dir>/<your-slot>-round-1.json` with keys: `reviewer_id`, `verdict`, `confidence` (0–1), `rationale` (≤180 words), `concrete_changes_required` (empty if APPROVE), `findings`.
