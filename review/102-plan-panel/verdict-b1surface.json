{
  "lens": "b1-removal-completeness",
  "decision": "CHANGES",
  "all_sites_in_plan": false,
  "missed": [
    "internal/setup/claude_test.go"
  ],
  "verification_avoids_harness_tests": true,
  "blocking": [
    "internal/setup/claude_test.go is NOT in Bead 1's changed-files (nor in spec Scope), but it asserts the PreToolUse `mindspec hook pre-complete` entry IS installed in ~6 tests (L47-48, L240-241, L567-569, L630-631, L678-679, and L768-769 'legitimate pre-complete gate entry should be present after cleanup'). Once wantedHooks() drops the entry per plan step 4, every one of these install-assertions inverts and the plan's own AC L257 / verification `go test -run 'TestSetup|TestClaude' -timeout 120s ./internal/setup/` will FAIL. The plan lists internal/setup/claude.go and internal/hook/hook_test.go but never internal/setup/claude_test.go, so the bead would leave a failing test it claims passes. Add internal/setup/claude_test.go to Bead 1 changed-files: rework the ~12 `mindspec hook pre-complete` assertions to assert the entry is now ABSENT / removed-by-omission (and that a pre-existing one is idempotently stripped), mirroring the spec-072 retired-hook test precedent."
  ],
  "summary": "All removed SYMBOLS (segmentInvokesComplete / strippedCompleteFields / completeBeadID / matchMindspecComplete / runPreComplete + helpers + SkipPanelEnv re-export) and ScenarioPanelGateBlocksPrematureComplete / scenario_panel_gate are confined to the files the plan lists (precomplete.go, precomplete_*_test.go, dispatch.go, hook.go, scenario_panel_gate{,_test}.go, scenario.go) plus the ADR-0037 enumeration ~L270 which step 7 covers — no dangling production-code ref would build-break. The precomplete.go helpers share names with internal/instruct functions (resolveBeadWorktree) but are package-local to `hook`, so no cross-package leak. The verification section is correct and SAFE: harness gets build/vet only (NEVER `go test ./internal/harness/...`), the 8 TestPanelGate live in internal/complete, TestNames_Sorted + setup tests are filtered. ONE blocking gap: internal/setup/claude_test.go is omitted from the changed-files yet hard-asserts the dropped hook IS installed in ~6 tests; AC L257 (`TestSetup|TestClaude` PASS) cannot pass without reworking it. Non-blocking nit: plan step 4 / changed-files says remove a `pre-complete` doc string in cmd/mindspec/setup.go, but that file has no `pre-complete` text (only generic 'PreToolUse hooks') — a no-op misdescription, not a missed removal. Comment-only `pre-complete` mentions in config.go/instruct/panel are non-load-bearing (optionally stale) and don't break the build."
}
